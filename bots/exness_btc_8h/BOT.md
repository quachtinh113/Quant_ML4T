# BOT.md — exness_btc_8h

The contract and memory of one bot. The builder updates it at the end of every phase; the mentor reads it before every
review. Keep it factual and dated. Numbers come from the registry, not from memory.

## Identity

| Field | Value |
|---|---|
| bot_id | `exness_btc_8h` |
| Instruments | `BTCUSD` (bare name; Market Watch name `BTCUSDm`, suffix `m`, via `bots/_shared/mt5_loader.py:SYMBOL_MAP`; profile `bots/assets/BTCUSD.md`) |
| Magic number | **`260904`** (`bots/_shared/__init__.py::MAGIC_ALLOCATION`, decided 2026-09-07; uniqueness asserted by `bots/tests/test_magic_numbers.py`). Distinct from the legacy bot's `202500`, **STOPPED 2026-09-10** (see Open questions 1) — see also Decisions log 2026-09-10 |
| Template case study | **`exness_gold_sess`** (Route B fork, 2026-09-08). **NOT `crypto_perps_funding`**, which `bots/README.md` names — see Decisions log 2026-09-08 "Template" |
| Research dir | `case_studies/exness_btc_8h/` — stages `01`–`05`, `config/`, `README.md`, and two shared modules that are not stages: `_features.py` (the one decision grid, price panel and feature construction) and `_model_reading.py` (how a ONE-NAME panel is read). `run_log/` and `benchmark/` are EMPTY: no model has been fitted and no backtest run |
| Experiment dir | `experiments/exness_btc_8h/` — the **running** one is the WSL2 copy, `~/ml4t/experiments/exness_btc_8h/`, created 2026-09-08 by `scripts/create_experiment.py`. Labels, features, model-based features and the evaluation ledger were written there; its `run_log/` now carries 86 training runs / 356 prediction sets / 0 backtests as of 2026-09-10T17:23Z |
| Tools | `bots/exness_btc_8h/tools/measure_btc_costs.py` and `measure_btc_weekend_spread.py` — read-only MT5 measurement scripts (Windows only, no `order_send` anywhere in either) |
| Tests | `bots/exness_btc_8h/tests/` — `test_lookahead.py` (10), `test_data_quality.py` (15), `test_cfd_pair_class.py` (20, **45 passed 2026-09-08**); `test_phase4_declarations.py` (18, added 2026-09-10, extended 2026-09-11 with the kill_criteria comment guard); `test_session_filter_diff.py` (20), `test_price_config_diff.py` (19), `test_entity_cols_diff.py` (15) — added 2026-09-10 for the fleet mentor gate and its combined re-gate (4 materials), all real assertions, none hard-coded to pass. **Full suite from WSL2 against the Windows tree (`ML4T_OUTPUT_DIR=~/ml4t/experiments/exness_btc_8h uv run --project ~/ml4t --no-sync --with pytest==9.0.3 python -m pytest bots/exness_btc_8h/tests -q`), as of 2026-09-11: 113 passed, 4 skipped, 0 failed** — every skip reads an honest "data not built in this environment", not a masked failure (fleet mentor material #4) |
| Account | Exness **Pro** (user decision 2026-09-05: no commission, costs are spread + swap); measured on the **demo** login `206539306` @ `Exness-MT5Trial7` (`trade_mode 0`, USD, leverage 1:2000, balance 899.40 USD on 2026-09-08); real account not yet read. **2026-09-10, first reading**: equity read at **891.15 USD**, down from 10,000 USD on 2026-09-05 — drawn down by the legacy bot (magic `202500`), now stopped (see Decisions log 2026-09-10). **2026-09-10, RE-FUNDED**: user reset the demo in the Exness Personal Area; equity now **10,891.15 USD**. This CLOSES Open question 2's capital-viability concern: 0.01 BTC (≈789 USD notional) is now ≈7 % of equity, not ≈88 % |
| Legacy | `bots/exness_btc_8h/legacy_v9_continuum/` (gitignored, pruned 2026-09-05 to 23 reference files; pristine original at `D:/05_Quant/_archive/v9_continuum_original`; see Decisions log). Legacy bot (magic `202500`) **STOPPED 2026-09-10** — see Decisions log 2026-09-10 and Open questions 1 |
| Owner | |

## Trading problem (mirrors `setup.yaml`)

| Field | Value |
|---|---|
| Decision cadence | 8-hour, **seven days a week** (`decision.cadence: 8_hour`, 1,095 slots a year) |
| Decision snapshot (UTC and server time) | 00:00 / 08:00 / 16:00 UTC. The server clock is UTC+0 all year (measured 2026-09-07 22:52:55 on `BTCUSDm`), so server time = UTC and the three instants do not move with any venue's daylight saving. The decision instant is the **close** of an 8-hour bar and the panel is keyed on it, so the `timestamp` column of every artifact IS the decision instant |
| Execution delay | `next_bar_open` — and on a 24/7 tape that is the decision instant itself. **Measured**: the gap between a decision and the bar it fills on is `[0]` minutes on all 9,356 slots. The fill price is the next bar's open against the decision bar's close: mean +0.029 bps, median +0.037, p90 \|gap\| 1.40 bps, max 209 bps |
| Session filter (if session bot) | `session_filter_values: [all, no_swap_night, us_hours]` — **masks, not a partition**: `all` = every slot, `no_swap_night` = {00, 08}, `us_hours` = {16}. Declared; **none has been run** |
| Primary label | `fwd_ret_8h` — decision close to the next decision close. Sealed 2026-09-08, digest **`7af4f668e867e91c`**, 9,357 rows (9,356 non-null), 8,260 in the development window |
| Other labels | `fwd_ret_24h` (3 slots), digest **`72df9e55bc4029a9`**, 9,357 rows (9,354 non-null). No classification label in this generation |
| Walk-forward (`n_splits`, `train_size`, `val_size`) | **4 × (P730D train, P365D validate)**, purge 3 decision slots (= `labels.buffer: 24H`) at every boundary. Measured: 2,187 training and 1,092–1,095 validation slots a fold; oldest training window opens **2019-09-03**, leaving **1,691 slots of warmup**; last validation ends 2025-08-30. Sizes are in DAYS and not years — see Decisions log "P2Y is not a year" |
| Holdout window | **2025-09-01 → 2026-08-31**, 1,095 slots. Declared 2026-09-08 in `config/setup.yaml::evaluation`, **before any label, feature or model existed**; never moved, never opened. Contamination flag set: the legacy bot trades `BTCUSD` on the same login (Open questions 1) |
| Assumed costs (spread p50 / p90 by session, commission, swap) | **Two readings, measured 2026-09-08, and they differ by 11×.** *Live (3,710,378 ticks over 30 days)*: a flat **1,000 points = 10.00 USD**, p50 1.28–1.29 / p90 1.57–1.59 bps, **identical in every session bucket, at the swap rollover, and at the weekend** (Sat p90 1.587, Sun 1.587 vs weekday 1.572); round trip p90 **3.18 bps**. *In sample (per-bar `spread` field, 8,261 development slots)*: p50 **6.32** / p90 **17.40** bps, round trip **34.8 bps**, because the quote is constant in POINTS while the price rose 15×. `costs.spread_bps` carries the in-sample range — it is what the engine charges. Commission **0** (Pro). **Swap: −1,638.6 points/lot/night long, 0.0 short, charged EVERY night including weekends, ×3 on FRIDAY** (`swap_rollover3days = 5`; FX and metals on the same account report 3 = Wednesday) = **−2.08 bps of notional a night**, **−9.7 %/year** at the measured mid |
| Position limits (`LiveRiskConfig`) | Not declared — phase 8. **Arithmetic, updated 2026-09-10 after the demo re-fund**: `volume_min` 0.01 lot × `contract_size` 1 = 0.01 BTC ≈ **789 USD of notional** against the demo account's **10,891.15 USD of equity** (was 898 USD before the re-fund), i.e. the smallest placeable order is now ≈7 % of the book, not ≈88 % — Open question 2 closed |

## Hypothesis

**APPROVED VERBATIM by the user 2026-09-10** (recorded by `quant-orchestrator`; drafted by `ml4t-mentor`,
source `D:\05_Quant\merg\exports\campaign_2026-09-10\btc_8h_phase1_draft.md` §B1, read-only). Copied verbatim below,
Vietnamese as drafted.

> BTCUSD trên tài khoản CFD này là một tên duy nhất, nên bot không tìm lợi thế xếp hạng. Lợi thế, nếu có, là
> (i) hồi quy ngắn hạn trên lưới 8 giờ: quan hệ duy nhất đo được ở phase 3 là đảo chiều một ngày (`slot_zscore_3`,
> `slot_ret_3`: IC −0.041, cùng dấu 4/4 fold, HAC t ≈ −2.5, bootstrap loại 0) — nhưng nó không qua BH trên 73 cột
> và chỉ bằng 0.36× IC* trong mẫu (0.1144 trên validation; 0.0954 trên toàn cửa sổ), nên tự nó không trả nổi chi phí
> lịch sử; và (ii) điều kiện hoá quan hệ đó bằng trạng thái đã khai — biến động và vị trí trong năm (D1), beta với
> USTEC/vàng/USD, và lịch swap — với kỳ vọng rằng một mô hình trên 68 cột làm được điều không cột nào làm được một
> mình. Phía bên kia là dòng lệnh phải giao dịch bất kể giá: thanh lý dây chuyền trên sàn perp có đòn bẩy, dòng
> tạo/hoàn ETF giao ngay Mỹ, và dòng xoay quanh mốc funding 00/08/16 UTC (`bots/assets/BTCUSD.md:77-81`) — đúng ba
> mốc quyết định của bot; CFD không có funding nhưng giá của nó theo giá đó. Lợi thế bền nếu các dòng ấy là cấu trúc
> của venue perp chứ không phải của broker này. Chi phí là phần của tín hiệu: spread trong mẫu p50/p90 6.32/17.40 bps
> (khứ hồi 34.8), spread tick hôm nay 1.29/1.58, swap long −2.08 bps/đêm mọi đêm kể cả cuối tuần, ×3 thứ Sáu
> (−9.7 %/năm), short 0 trên demo — nên chiều long ở mốc 16:00 phải vượt thêm một đêm swap mà chiều short không phải
> trả. Giả thuyết bị bác bỏ khi, tại K cộng dồn ghi trong bảng Trials (K5 = 516), không spec nào có DSR ≥ 0.95 trên
> active return so với sổ long BTCUSD cùng cửa sổ phiên, đã trừ swap, hoặc khi breakeven từ `16_costs` thấp hơn
> `breakeven_gate` (`setup.yaml:460-464`: khứ hồi p90 trong mẫu + swap × số đêm giữ, biên 1.5×) đo trên tài khoản
> Pro thật. Prior phải vượt qua: `crypto_perps_funding` không có chiến lược end-to-end; ba bot cùng broker chạy
> 3,204 / 2,514 / 1,780 trial không có survivor; phase 3 của chính bot này 0/73. Holdout 2025-09-01 → 2026-08-31
> chỉ xác nhận, không chọn, vì legacy 202500 đang giao dịch BTCUSD trên cùng login.

**CORRECTION 2026-09-10 (before any stage forked or fit), flagged by the coordinator, does not alter the approved
text above:** the blockquote's "K5 = 516" is the population from `|declared configurations|` (43/label), not from
`|prediction sets|`. Every GBM preset this menu resolves to (`case_studies/config/lgb/*.yaml`) declares
`checkpoint_interval: 50, max_iterations: 500` = 10 checkpoints, and each checkpoint publishes its own
`prediction_hash` (`case_studies/research/model_planning.py::checkpoint_schedule`) — the same arithmetic
`bots/exness_gold_sess/BOT.md` phase 4 measured ("15 configs x 10 checkpoints x 2 labels = 300 sets") and
`reviews.jsonl` 2026-09-10 records for `exness_fx_d1` generation-2 (K=2,136). Corrected: prediction sets/label =
28 + 15×10 = **178**; phase-4 total (2 labels) = **356** (what R1 below counts against, not 86); K5 (phase-5
backtest specs) = 178 × 2 labels × 3 session books × 2 signal specs = **2,136** (not 516). The falsification
line's *logic* is unchanged — "no spec clears DSR ≥ 0.95 at the cumulative K" — only the K it is read against
moves. See `config/training/{fwd_ret_8h,fwd_ret_24h}.yaml` and `config/setup.yaml::backtest.sweep` for the
corrected derivation, and the Trials table below for the corrected entry.

Mức kèm theo (người dùng quyết): `confidence_level = 0.95` (`16_strategy_simulation/12_dsr_validation.py`); báo cả
raw và active; active là bắt buộc vì buy-and-hold của tài sản trên cửa sổ phát triển 2019-09-03 → 2025-08-31 có
drift dương mạnh (data lake `price_stats`, as_of 2025-08-31, gross: log drift 0.39/năm, vol 0.62, Sharpe 0.63 ± 0.45,
max DD −77 %).

**Cross-bot note (mentor edit #10, xau_fx_mt5 trial-4 screen, 2026-09-10; UPGRADED 2026-09-10 by the phase-4
closure gate):** the dollar-numeraire hypothesis for BTC — family `dollar` (`config/setup.yaml:648-656`, priced
but not traded, EURUSD/GBPUSD/USDJPY/AUDUSD/USDCAD signed proxy) — lives **HERE**, in `exness_btc_8h`, and
nowhere else. Status upgraded from "scored" to **"scored AND spent"**: phase 3 (2026-09-08) measured
`usidx`/`dollar`-adjacent columns including **`usd_beta_63d` IC +0.0265, HAC t 1.85, fdr_p 0.945** (survives
neither BH nor, on its own, a naive |t|>=1.96 screen once corrected), and the SAME `dollar` family's columns then
rode inside the 68-column feature matrix every one of the 356 phase-4 prediction sets (`06_linear`/`07_gbm`,
generation 1) was fit on — the family did not just get measured univariately, it was an active input to the
model population phase-4's R1 gate just CLOSED (see §Trials, "Phase-4 closure"). It was explicitly **NOT opened**
in `xau_fx_mt5`: that bot's proposed trial #4 ("DXY as entry feature") was screened out 2026-09-10 — `DXYm`
history on this account starts only 2021-05-19 (misses folds 2-3 train start 2019-08-26), no exogenous
full-history dollar index exists in the lake, and every synthetic construction would duplicate this bot's
already-scored `dollar` family. **Any fleet pre-registration may no longer cite "the dollar family for BTC is
untested"** — it is tested, spent, and closed inside this bot's generation 1. Source: `reviews.jsonl`
2026-09-10T13:28:39Z (`quant-orchestrator`, phase `trial-4-screen`, bot `xau_fx_mt5`) and
2026-09-10T17:48:20Z (`quant-orchestrator`, phase `"4 (gate: screen met, generation 1 closed)"`, this bot).

## Kill criteria

**APPROVED VERBATIM by the user 2026-09-10** (recorded by `quant-orchestrator`; drafted by `ml4t-mentor`,
source `btc_8h_phase1_draft.md` §B2). Operational threshold (a) drawdown = **8 % of allocated capital**
(user decision). **R1 is an EARLY-CLOSE GATE — the optional clause is ADOPTED**: if 0/**356** prediction sets have
HAC p < 0.05 after BH at phase 4, the generation closes and the **2,136** phase-5 backtests are **NOT run**.
**Counts corrected 2026-09-10** from the draft's 86 / 516 (see the Hypothesis section's correction note above);
the rule itself is unchanged.

### Nghiên cứu (phase 4-5-7)

- **R1** (phase 4, báo cáo, không phải cổng chọn — `roadmap.md:297`): mỗi prediction set in pooled OOF IC (HAC) so với
  IC* 0.1144 và sign consistency 4 fold. *Tuỳ chọn — ĐÃ DUYỆT 2026-09-10, ÁP DỤNG làm early-close gate*: nếu 0/**356**
  prediction set (28+15×10 GBM checkpoint mỗi nhãn × 2 nhãn — sửa 2026-09-10 từ 86, xem ghi chú sửa ở mục Hypothesis)
  có HAC p < 0.05 sau BH thì đóng thế hệ, không chạy **2,136** backtest.
  **M2 (mentor gate 2026-09-10, note dated 2026-09-10, CHƯA giải quyết):** văn bản đã duyệt ở trên nói **chỉ BH**
  ("có HAC p < 0.05 sau BH") là điều kiện đóng thế hệ. `setup.yaml::kill_criteria` và `12_model_analysis.py`'s
  `clears_r1` lại cài đặt **BH VÀ sign_consistency = 4/4** — rộng hơn văn bản đã duyệt (một điều kiện phụ không có
  trong B2). Kết quả HÔM NAY giống hệt dù dùng điều kiện nào (`n_bh_significant = 0` ngay cả khi bỏ điều kiện sign
  — không dòng nào qua nổi bước BH trước, nên sign consistency chưa từng là yếu tố quyết định lần này), nhưng đây
  là một lệch thật giữa code và văn bản đã duyệt, PHẢI được giải quyết (người dùng sửa văn bản B2 để thêm điều
  kiện sign, HOẶC code thu hẹp lại còn đúng BH) **trước khi bất kỳ generation 2 nào được mở** — không được để lệch
  âm thầm sang lần chạy sau, khi kết quả hai điều kiện có thể khác nhau.

  **M2 RESOLVED 2026-09-11 (user decision Q1, recorded by `quant-orchestrator` in `reviews.jsonl`
  2026-09-10T22:10:25Z / UTC 2026-09-10T22:10Z; option chosen: B — narrow the code to the
  pre-registered B2 text above):** `12_model_analysis.py:374-382` — `clears_r1` is now
  `pl.col("bh_significant").alias("clears_r1")`, BH significance alone, matching lines 101-102 and
  109-111 above verbatim ("có HAC p < 0.05 sau BH" — no AND clause). `sign_consistency` /
  `r1_required_sign_consistency` are still computed and REPORTED (verdict JSON keys
  `sign_consistency_reported_threshold`, `n_sign_consistent_4of4_reported`; verdict JSON now also
  carries `gate_condition: "bh_fdr_only"`) but no longer enter the gate. `setup.yaml::kill_criteria.
  r1_required_sign_consistency` is unchanged in value (1.0), its comment reworded "reported, NOT
  gating". Stage 12 re-run in WSL2 to prove the fix moves no result: verdict byte-identical on every
  outcome number — `n_prediction_sets: 356`, `n_bh_significant: 0`, `n_clears_r1: 0`,
  `best_ic_mean: 0.030302525019830308`, `generation_closed: true` — registry unchanged (86 training
  runs, 356 prediction sets, 0 backtests). See Decisions log 2026-09-11 and §Trials for the full
  proof.
- **R2** (phase 5, cổng): tại K = **2,136** (sửa 2026-09-10 từ 516) (`member_digest` ghi vào BOT.md), số spec có DSR ≥ 0.95 trên active return = 0
  → phase 6 không mở, holdout không đọc. In `variance_trials` và `expected_max_sharpe` TRƯỚC bảng Sharpe.
  `n_trials_effective_*` chỉ chẩn đoán, không hạ K.
- **R3** (phase 5, cổng phơi nhiễm): spec ĐẠT raw nhưng KHÔNG ĐẠT active → "không phân biệt được với giữ long";
  báo tỷ lệ phiên `|net|/gross > 0.5`.
- **R4** (phase 6): `16_costs` chỉ chạy khi `costs.swap.measured_on_account: real` → breakeven (bps/lượt khớp)
  < 1.5 × (17.40 + 2.08 × đêm giữ trung bình của spec) → loại spec.
- **R5** (phase 7, một lần): PSR active vs sổ long BTCUSD trên holdout < 0.5 → không triển khai; holdout đã cháy nếu
  quay lại sửa thiết kế.

### Vận hành (viết trước lệnh live đầu tiên; ánh xạ vào `bots/exness_fx_d1/monitor/circuit_breakers.py` và
`bots/_shared/monitor/account.py`)

- (a) Drawdown từ đỉnh > **8 %** vốn phân bổ — *người dùng quyết con số*: σ một slot ≈ 190 bps, σ một tuần ≈ 870 bps,
  nên 8 % trên sổ 100 % là một tuần bình thường của tài sản; ngưỡng chỉ có nghĩa cùng với sizing. Tầng tài khoản
  hiện 6 %/2 % (`bots/_shared/monitor/account_limits.yaml:14`), bot này "not yet declared" (`:24`).
- (b) IC lăn 63 quyết định (21 ngày) của mô hình đang chạy < 0 suốt 63 quyết định liên tiếp.
- (c) Chi phí khứ hồi thực hiện > 1.5 × 3.18 bps (`setup.yaml:276`) trong một tháng dương lịch — `RealisedCostBreaker`.
- (d) Swap thực tế/đêm lệch > 10 % so với −1,638.6 điểm, hoặc `swap_short` ≠ 0 xuất hiện, trong một tuần — `SwapBreaker`.
- (e) Reconciliation thấy vị thế BTCUSDm không mang magic 260904 (tức 202500) khi bot đang giữ hoặc sắp đặt lệnh →
  không đặt lệnh chu kỳ đó — `ReconciliationBreaker` + `OpenExposureBreaker`.
- (f) Nến quyết định thiếu trong dung sai → không lệnh slot đó — `MissingDecisionBarBreaker`.
- (g) `volume_min × giá > vốn phân bổ × trọng số mục tiêu` → không lệnh; khai `capital.min_viable_allocated`
  (tiền lệ `exness_usidx_sess/deploy/deployment_loop.py:1035`). **Cập nhật 2026-09-10 sau khi demo được nạp
  lại**: 789 USD/0.01 BTC so với equity hiện tại 10.891,15 USD (trước đó 898 USD) — sizing có khoảng mười ba
  bậc dưới full size, không còn một bậc duy nhất; Open question 2 đã đóng.
- (h) Spread tại mốc quyết định > 3 × 1.58 bps → không lệnh slot đó.
- Nghỉ hưu: breakeven tái ước lượng < khứ hồi live + swap; hoặc R5; hoặc (c) trip ba tháng liên tiếp.

## DSR benchmark

**APPROVED VERBATIM by the user 2026-09-10** (recorded by `quant-orchestrator`; drafted by `ml4t-mentor`,
source `btc_8h_phase1_draft.md` §B3).

- Công thức `12_dsr_validation.py:107-114, 147-169`; `is_significant = probability ≥ 0.95`; in cả notebook và
  `ml4t.diagnostic`. PPY = **365** (`setup.yaml:768`), không 1,095.
- K5 = **2,136** = 178 prediction set/nhãn (28 linear + 15×10 GBM checkpoint) × 2 nhãn × 3 sổ × 2 signal spec —
  **sửa 2026-09-10 từ 516** (516 dùng |config| thay vì |prediction set|; xem ghi chú sửa ở mục Hypothesis và
  `config/training/{fwd_ret_8h,fwd_ret_24h}.yaml`). K6 = 2,136 + survivors × 2 nhãn × 8 risk control × n arm
  vol-target (n phải liệt kê trong `setup.yaml` TRƯỚC fit đầu tiên — quy ước fleet
  `bots/exness_gold_sess/PHASE567_DECLARATION.md:266`: risk overlay là trial, cost grid không; n = **3** vol-target
  arms, declared `setup.yaml::backtest.sweep.position_sizing.vol_target_arms`, 2026-09-10). On-chain / triple-barrier
  / short_only là +K khi mở.
- Benchmark cho active return: sổ **long BTCUSD** trên cùng cửa sổ phiên của mỗi sổ (`all`: long liên tục;
  `no_swap_night`: long 00→16, flat 16→00; `us_hours`: long 16→00), gấp lưới 8h về ngày trước khi `pct_change`,
  **trừ swap long**, báo cả gross. Dựng trong `_report_phase5.py` vì `benchmark/` của stage 01 rỗng — khai chỗ dựng.
- Không thêm sổ inverted (một trial nữa, rò dấu); "random signal" = expected-max dưới null của DSR.

## Phase status

| Phase | Status (met / gap / n.a.) | Date | Evidence (registry hash, file, notebook) |
|---|---|---|---|
| 0 Environment | **met** | 2026-09-08 | `scripts/verify_installation.py` on WSL2 `~/ml4t`: **PASS, 148/154 required checks**, 131.6 s. Core DS 7/7, viz 8/8, ML 24/24 (lightgbm 4.6.0, xgboost 3.2.0, catboost 1.2.10), DL 8/8, ML4T packages and sub-modules 5/5, repo packages 10/10, runtime 9/10 (only skip: no GPU), Python 3.14.7, `ML4T_DATA_PATH` → `/mnt/d/.../data`. 5 optional Docker-only extras absent (`gensim`, `signatory`, `arcticdb`, `clickhouse-connect`, `influxdb-client`) — none used by phases 0–3. **The 2026-09-05 row saying "`uv sync` incomplete; `ml4t.*` packages missing" was a Windows conclusion and is superseded**: the project env does not build on Windows (`docs/installation.md`) and every stage of this bot runs in WSL2. **Not available**: every API key in `.env` has an EMPTY value — checked by VALUE, not presence: `FRED_API_KEY`, `EDGAR_IDENTITY`, `QUANDL_API_KEY`, `DATABENTO_API_KEY`, `OANDA_API_KEY`, `ALPACA_*`, `POLYGON_API_KEY`, `TABPFN_TOKEN` are all present as lines and empty as values. Consequence: no macro family can be built, and the `on-chain and flow` register entry stays PLANNED — **wording corrected 2026-09-11**: the repo does have two free, key-free sources already wired (DefiLlama/CoinGecko for TVL, `data/crypto/onchain/`; Binance perp funding/premium, `crypto_perps_funding/funding_data.py`), but neither supplies the three variables this family names (exchange netflow, miner reserves, US spot-ETF creations) and neither carries a vintage — see `setup.yaml:733` (comment corrected the same day) and GEN2_DECISION_DRAFT_2026-09-11.md §"Đối chiếu closure record với artefact". **Windows side**: `MetaTrader5` 5.0.5640 and a logged-in terminal are available under the isolated Python 3.12; the project `.venv` on Windows has neither `polars` nor `MetaTrader5`, so MT5 work runs through `uv run --no-project --python 3.12 --with ...` |
| 1 Strategy and learning task | **met** | 2026-09-08 | `case_studies/exness_btc_8h/` created as a Route B fork of `exness_gold_sess` (**not** `crypto_perps_funding`). `utils.paths.get_case_study_dir("exness_btc_8h")` verified to resolve **before** anything was copied (→ `case_studies/exness_btc_8h`, and → `experiments/exness_btc_8h/exness_btc_8h` under `ML4T_OUTPUT_DIR`); the id is not in `STRATEGY_IDS` and does not need to be — nor is `exness_fx_d1` or `exness_gold_sess`. `config/setup.yaml` filled from measurement: universe `BTCUSD`, `history_start` 2018-02-16 (measured), cadence `8_hour`, snapshots 00/08/16 UTC, `execution_delay: next_bar_open` with a measured 0-minute gap, server clock measured, spreads measured from this account's ticks AND from the per-bar record, swap read from `symbol_info` with the crypto rule. **Folds and holdout declared in `setup.yaml` on 2026-09-08 before any fit existed.** `01_feasibility_analysis` and `02_labels` ran green in `~/ml4t/experiments/exness_btc_8h`; labels sealed at `7af4f668e867e91c` / `72df9e55bc4029a9` |
| 2 Point-in-time data | **met** | 2026-09-08 | `bots/exness_btc_8h/tests/` — **45 passed**. `test_lookahead.py` (10): the 8h fold reproduces the sealed label on 8,261 slots at max \|diff\| **0.0**; the whole 68-column matrix rebuilt from only the bars closed at 6 sampled decision instants equals the batch row at max \|diff\| **0.0**; withholding everything from 2023-01-01 moves nothing on 5,339 × 68; the D1 asof join is backward with the bar's age exactly {0: 0h, 8: 8h, 16: 16h}; the two swap flags agree with `costs_mt5.holding_cost_points` on 3,000 holds; no cross-sectional and no constant column. `test_data_quality.py` (15): 8,261 development slots, **0 missing**, **0 folded from a single H4 bar**, 0 OHLC violations, 0 zero-volume bars, 0 zero-spread bars, repeated close 0.0121 %, H4 opens exactly {0,4,8,12,16,20} UTC (which refutes the naive-datetime download defect for this symbol), slots spread evenly over all **seven** weekdays, 19 moves > 10 % and none > 60 %. `test_cfd_pair_class.py` (20): the shared-code fix and its regression guard |
| 3 Features | **met** | 2026-09-08 | Register declared in `setup.yaml::features` **before any IC was read**; `03_financial_features` → `features/financial.parquet` digest **`826fc5e318a17217`**, 8,431 rows × **68** columns, 7,335 development rows, warmup cut on `d1_zscore_63d`, only the declared `usidx_*` family null (9.3–11.0 %, inside the declared 12 % cap). `04_model_based_features` → `features/model_based.parquet` digest **`a7a08b65989898ca`**, 20,784 rows × 5 columns over 5 folds (4 validation + 1 holdout vintage), both models filtered-not-smoothed with a 0.00e+00 drift assertion. `05_evaluation` → `evaluation/ic_timeseries.parquet` (319,521 scored slots) and `evaluation/triage_ledger.parquet` (73 rows). **`run_log/` is empty: 0 trials** |
| 4 Models | **CLOSED 2026-09-11 — generation 1 closed by R1; generation 2 CONDITIONAL** on (1) a user data decision for the `on-chain and flow` family (vendor, key in `.env`, backfill 2018-02-16 → 2025-08-31, vintage) and (2) a new user-approved B1 predicting `fwd_ret_8h` pooled IC ≥ 0.062 (m = 36) / ≥ 0.071 (m = 356) at HAC bandwidth 9, median hac_se 0.01937 (8h 0.01535); no tuning; dollar family scored AND spent; user declined on-chain sourcing 2026-09-11 (Q3: not now). **FIT COMPLETE 2026-09-10T17:25:50Z — R1 EARLY-CLOSE GATE TRIPPED, generation CLOSED. 0/356 prediction sets clear BH p<0.05 AND sign_consistency=1.0 (0/356 BH-significant alone). Phase 5 (`13_backtest`, K5=2,136) does NOT open this generation, per `kill_criteria.r1_early_close: true`** (M2 resolved 2026-09-11, option B: post-fix the gate is BH alone, still 0/356 clears; see §Kill criteria "M2 RESOLVED 2026-09-11" and §Trials 2026-09-11) | 2026-09-10 | Hypothesis, Kill criteria and DSR benchmark approved verbatim by the user 2026-09-10 (see §Hypothesis, §Kill criteria, §DSR benchmark); `bots/exness_btc_8h/tests` green (see Decisions log 2026-09-10). Training menus declare 43 configurations a label, but the **prediction-set count is 178/label** (28 linear + 15 GBM × 10 checkpoints each), corrected 2026-09-10 from a first-pass 43/86/516 — see the Hypothesis section's correction note. K5 = **2,136** backtest specs when phase 5 opens, **still not run — 13_backtest was not attempted this round under any outcome, per instruction**. `06_linear.py`, `07_gbm.py`, `12_model_analysis.py` and `13_backtest.py` **forked from `exness_gold_sess` 2026-09-10**, adapted for one instrument (no cross-sectional ranking), the 8h grid, PPY 365, three session books and two signal specs; R1 wired declaratively as `kill_criteria.r1_early_close: true` in `setup.yaml`, evaluated in `12_model_analysis.py` before it hands off to `13_backtest`. `tests/overrides.yaml` extended with 4 new skip entries; 16 new static tests in `bots/exness_btc_8h/tests/test_phase4_declarations.py`. Synced to `~/ml4t/case_studies/exness_btc_8h/` and `~/ml4t/bots/exness_btc_8h/` (`rsync`, no `--delete`, `pyproject.toml` excluded); full bot suite **61 passed** in WSL2 against the synced copy with the pre-existing experiment artifacts, the 16 new static tests also pass standalone on native Windows. **THE APPROVED FIT WAS ATTEMPTED 2026-09-10T14:25Z and FAILED at `06_linear.py` cell 14 after 12.3 s**: `ValueError: linear runner requires timestamp and an entity key`, raised from `utils/modeling.py::load_modeling_dataset` (line ~924), a SHARED, top-level function every Ch11+ case study's linear/GBM runner goes through. Root cause: that function drops any join column with `n_unique() <= 1` from `entity_cols` to avoid "collapsing all entities into one group" on a cross-sectional panel — but on this bot's genuinely ONE-instrument panel `symbol` is constant EVERYWHERE, so the filter strips `entity_cols` to empty and `_load_batch_base` (`case_studies/utils/linear.py:544`) then raises. **No prior Exness-fleet bot hit this path**: `exness_gold_sess` has 2 instruments, `exness_fx_d1` has 5, `exness_usidx_sess` has 2 — `exness_btc_8h` is the first and only ONE-instrument bot in this fleet to reach this function. Proposed diff (NOT applied — `utils/modeling.py` is shared, same policy as `case_studies/utils/*`) in Open questions, new item 14. **Zero training runs registered**: the failure occurred inside `ModelRequest.resolve()`, before `run_model_population` fits anything; `~/ml4t/experiments/exness_btc_8h/exness_btc_8h/run_log/registry.db` now exists (created by `Study.open`'s adopt-path) but carries 0 `training_runs` / 0 `prediction_sets` rows — this is the ONE side effect of the attempt, and it moves no K. A SECOND, independent reason this round could not proceed: `pgrep -af papermill` at start time showed `exness_fx_d1`'s `13_backtest` (`13_backtest_gen3_cross_sectional.ipynb`) ACTIVELY RUNNING (PID 822, started 21:23Z, still running when checked again after the 06_linear failure) — contradicting the "WSL2 is idle" premise; per instruction ("if one is, wait — do not run both") this alone would have required waiting, independent of the shared-library blocker. Full log: `~/ml4t/btc8h_phase4.log`. Exact fit command once BOTH blockers clear (WSL2): `cd ~/ml4t && LD_LIBRARY_PATH=/home/pro_trader/omp:$LD_LIBRARY_PATH ML4T_OUTPUT_DIR=~/ml4t/experiments/exness_btc_8h uv run python case_studies/exness_btc_8h/06_linear.py` then `07_gbm.py` then `12_model_analysis.py`, campaign rule `as_of <= 2025-08-31` for every data call. **Expected wall-clock**, read from `exness_gold_sess`'s own record: `06` about 4 min, `07` about 9 min for 300 GBM sets (this bot's GBM population is the same shape: 15 configs × 10 checkpoints × 2 labels = 300). A separate blocking dependency for phase 5 (`13_backtest`) is recorded in Open questions item 5 (`apply_session_filter` / `_PRICE_CONFIG`, diffs proposed, not applied). **UPDATE, same day, later:** all three diffs (Open questions 5 and 14) went through the fleet mentor gate, were rewritten per the mentor's findings, and were APPLIED on the Windows tree once the orchestrator said "go" — `bots/exness_btc_8h/tests` **98 passed, 4 skipped** (skips are `fx_pairs`/`etfs` data absent in this sandbox). The `06_linear` blocker (`utils/modeling.py`) is CLEARED; the `13_backtest` blockers (`apply_session_filter`, `_PRICE_CONFIG`) are also CLEARED. **Still true**: `06`/`07`/`12`/`13` have not been RUN (only their shared-library dependencies were fixed and proven by unit test); the fit itself remains queued, not fitted, K unmoved at 0. **Materials applied, same day, combined re-gate**: the four non-blocking materials the fleet mentor requested on top of the MET diffs (`session_filter_all_value` declaration, `utils/modeling.py` n_assets/symbols assert, `_naive_end_date_string` end_date coercion, tri-state skip probe in `test_entity_cols_diff.py`) are all applied and verified — WSL2/`ML4T_OUTPUT_DIR` run 4 failed/107 passed/4 skipped (failures are a stale, unsynced experiment config, not the materials), native run 100 passed/15 skipped/0 failed; see Decisions log 2026-09-10 for both. **THE FIT RAN, 2026-09-10T17:17:31Z → 17:25:50Z (8m19s wall-clock), WSL2, sequential, background, `~/ml4t/btc8h_phase4.log`**: `06_linear.py` OK in 84.3s (56 training runs, 56 prediction sets: 28 linear × 2 labels, exactly as declared); `07_gbm.py` OK in 300.6s (30 training runs, 300 prediction sets: 15 GBM configs × 10 checkpoints × 2 labels, exactly as declared); `12_model_analysis.py` reached cell 15 (R1) and RAISED at 107.6s into its own run — **this is the designed, correct outcome for a closed generation, not a bug**: `RuntimeError: R1 EARLY-CLOSE GATE TRIPPED: 0 of 356 prediction sets clear BH p < 0.05 AND sign_consistency >= 1.0`. Verdict persisted: `~/ml4t/experiments/exness_btc_8h/exness_btc_8h/evaluation/r1_early_close_verdict.json` — `n_prediction_sets: 356, n_bh_significant: 0, n_clears_r1: 0, best_config: "default_mse", best_ic_mean: 0.030303 (GBM, fwd_ret_8h, 26.49% of IC*=0.1144), generation_closed: true`. Registry, read after the run (`case_studies.utils.registry.load_training_runs`, `study.predictions.table()`): **86 training runs** (43 configs × 2 labels), **356 prediction sets** (178 × 2 labels, confirmed split 28 linear + 150 GBM per label) — matches the corrected K5 derivation exactly, no drift. Per-label/family best pooled IC: `fwd_ret_8h` gbm **+0.0303** (leader), linear −0.0017; `fwd_ret_24h` gbm +0.0120, linear −0.0266 — every one negative or far below IC*, none clearing BH. Newey-West: measured overlap `[0, 2]` slots → floor `[1, 3]`, automatic bandwidth applied `[9]` on both labels (the larger, conservative figure). `13_backtest` was NOT run, NOT attempted, under this outcome or any other, per instruction — no `backtest_runs`/`backtests` directory exists in `run_log/`, confirmed by directory listing. `06`/`07` moved K by their own population size once R1 is read against it (see §Trials); `13`/K5=2,136 stays undrawn |
| 5 Backtest and inference | **NOT OPENED, generation 1 CLOSED before this phase** | 2026-09-10 | `kill_criteria.r1_early_close` fired in `12_model_analysis.py` (0/356 prediction sets clear R1); `13_backtest.py` (forked, unit-tested, blockers cleared) was never run this generation and must not run — the closed-generation outcome the early-close gate exists to produce. K5 = 2,136 stays a declared, undrawn number |
| 6 Portfolio, costs, risk | **NOT OPENED** | | Downstream of phase 5, which did not open |
| 7 Holdout (scored once) | **not opened** | 2026-09-08 | Declared 2025-09-01 → 2026-08-31. Nothing in phases 0–3 read a row dated on or after it except the feature matrix, which is written through the holdout so the holdout stages have a vintage; every figure and statistic in 01–05 is asserted to be strictly before it |
| 8 Deployment (demo → shadow → live) | gap — shared adapter ready, this bot's `deploy/` not created | 2026-09-05 | `bots/_shared/mt5_broker.py` unit-tested and smoke-tested read-only on the demo terminal; magic `260904` allocated; `deploy/risk_config.yaml` and `deploy/deployment_loop.py` pending |
| 9 Monitoring and retirement rules | **not opened** — retirement rule not tripped: bot never live; closure is a research closure | 2026-09-11 | No `deploy/` run, no live or paper order ever placed (see Phase 8, gap); the operational retirement rule in §Kill criteria ("Nghỉ hưu: breakeven tái ước lượng < khứ hồi live + swap; hoặc R5; hoặc (c) trip ba tháng liên tiếp") requires a live book to trip against and none exists. Generation 1 ends by the pre-registered RESEARCH kill criterion R1 (phase 4, BH-only), not by an operational retirement condition |

## Decisions log

| Date | Decision | Why | Alternatives rejected |
|---|---|---|---|
| 2026-09-05 | Bot exists as one of four (4 bots, 6 models, 10 instruments) | one bot = one cadence, one cost model, one signal generator | 1 bot for all 10; 10 bots; a 5th session-FX bot |
| 2026-09-05 | The legacy bot copy is kept **per bot** as `bots/<bot_id>/legacy_v9_continuum/` (renamed from `v9 Continuum`, contents untouched, gitignored; user decision 2026-09-05). It may be read only as a *reference for MT5 connectivity code and symbol naming* (the suffix `m`). It is never evidence, an expectation or a kill criterion: it has no declared holdout, no Deflated Sharpe Ratio and no trial count | One legacy copy per bot lets each bot be adapted independently later; keeping it outside the evidence chain keeps the registry and the DSR honest | Deleting it; moving it out of `bots/`; using its backtest reports as a prior for this bot |
| 2026-09-05 | **Legacy archived and pruned** to a KEEP set of 23 files per bot (311 KB from 1962 files / 492 MB); pristine original at `D:/05_Quant/_archive/v9_continuum_original`; all 16 `.env*` files deleted first | The ML4T pipeline replaces the strategies, indicators, ML gate, backtester and retrain scripts; four 492 MB duplicates carrying credentials served no purpose, while the MT5 connectivity code is still worth reading | Keeping the full copies (secrets, size); deleting everything (loses the connectivity reference); one shared legacy directory |
| 2026-09-05 | **One shared MT5 adapter**, `bots/_shared/mt5_broker.py`, implements the `MockBroker` contract of `25_live_trading/10_safety_risk_demo.py` for every bot; what is per bot is the **magic number** (`260904` here), `deploy/risk_config.yaml` and `deploy/deployment_loop.py` | Four copies of `order_send` would drift; the contract is the same for every bot and only data differs | `bots/<bot_id>/deploy/mt5_broker.py` per bot; subclassing a shared base per bot |
| **2026-09-08** | **Template: fork `exness_gold_sess`, not `crypto_perps_funding`** | Three reasons read out of the case study itself, not preference: (a) its own README says "The complete release pipeline is not yet supported" and "Notebooks 13-17 are not a current end-to-end strategy"; (b) all six of its feature families read the perpetual's premium index or its funding settlements, so removing funding — which a CFD does not have — removes every feature it builds; (c) its `execution_mode: same_bar` fills AT the funding instant, which without funding is a one-bar lookahead. `exness_gold_sess` is already on MT5 history, a UTC+0 server, `features.ranked` empty by design, and labels named `fwd_ret_8h` / `fwd_ret_24h` with explicit `horizons` | Forking `crypto_perps_funding` as `bots/README.md` names; forking `exness_fx_d1` (cross-sectional throughout); Route C from scratch. **Kept from `crypto_perps_funding` as reference only**: the slot arithmetic 1,095/year, `calendar: crypto` with `periods_per_year: 365`, and the shape of `labels.rebalance_step` |
| **2026-09-08** | **`execution.share_type: fractional`**, settled before the first registry row | Forced by arithmetic unique to this instrument. The engine sizes under `integer` in whole units and one unit here is one Bitcoin ≈ 79,000 USD against 100,000 of equity, so the grain is ~79 % of the book: every target weight below 0.79 rounds to **zero**, the book places no order, and the sweep still completes and writes a full set of registry rows showing a flat curve. Compare EURUSD (~86,000 units, grain 0.001 %) and XAUUSD (~33 units, grain 3 %). It is also what the broker allows: `volume_min = volume_step = 0.01` lot × `contract_size 1` = 0.01 BTC | `integer` (the repository default and what every sibling bot uses) — a silent failure, not a loud one, which is why it is settled with `run_log/` empty |
| **2026-09-08** | **`costs.spread_bps` carries the IN-SAMPLE spread (6.32 / 17.40 bps), not today's tick reading (1.29 / 1.58)** | `backtest_loaders::_normalize_cfd_costs` turns this key into the engine's slippage leg, so it is what every Ch16-19 backtest of 2018-2025 pays. The broker quotes a near-constant number of POINTS while the price rose from 6,700 to 103,500, so the same spread is 14.6 bps in 2018 and 2.8 in 2025; charging today's number would understate the cost 11-fold, concentrated in the years the training data is in. The tick table stays in `costs.spread_bps_by_session` and is what a live order and the go-live gate read | Declaring the tick range (the obvious copy from the sibling bots); declaring one blended number; charging a per-year schedule (the engine takes one range) |
| **2026-09-08** | **`train_size: P730D` / `val_size: P365D`, not `P2Y` / `P1Y`** | Measured, not preferred. `evaluation.calendar: crypto` maps to **None** in `utils/cv_splits.py::_CALENDAR_MAP` — correct for a 24/7 market — and with no calendar the library reads the size as a pandas offset alias. `_normalize_duration` turns `P2Y` into `2YE`, a **year-end** offset, which produces ~320-day windows: measured on this grid, `P2Y`/`P1Y` gives 2,046-slot training and 951-slot validation windows starting **2020-04-25**, against 2,187 and 1,095 starting **2019-09-03** for the day form — 1.4 years of usable history thrown away and every validation year six weeks short, silently. `exness_fx_d1` and `exness_gold_sess` are unaffected: their `calendar: FX` maps to `CME_FX` and takes the session-counting path | `P2Y`/`P1Y` (what the coordinator's spec and every sibling bot declares — the *intent* is honoured exactly, the *encoding* is not); `n_splits: 5` (would put the oldest training start at 2018-09 and leave only 615 warmup slots) |
| **2026-09-08** | **All three decision slots kept; the 00:00 rollover collision is measured rather than avoided** | Two measurements taken before `setup.yaml` was written. (a) The spread does **not** widen at rollover: the `rollover` bucket (75,463 ticks, ±15 min of server midnight) is 1.29 / 1.58 bps, the same to 0.01 bp as every other bucket — so `bots/assets/BTCUSD.md` section 5's warning is falsified on this account and is recorded as falsified. (b) The swap does bite but bites the **16:00** slot, not the 00:00 one: it is the hold that crosses midnight that pays. Dropping a slot would remove a third of the sample to avoid a cost that is knowable in advance and can be carried as a feature | Dropping the 00:00 slot (removes a third of the sample for a falsified reason); dropping the 16:00 slot (removes the only US-hours slot, which is the one the asset profile says co-moves with USTEC); ignoring the collision |
| **2026-09-08** | **Swap is NOT in any label; it is two state features plus a cost-stage charge** | It is a holding cost per night, not a per-crossing cost the percentage engine can express; folding it in would make every label digest depend on the account type and give a Friday 16:00 decision a different quantity from a Tuesday 16:00 decision under the same name. Instead `pays_swap_night` and `pays_triple_swap` are knowable-in-advance state columns, checked against `costs_mt5.holding_cost_points` on the real record, and the breakeven gate is `round_trip_p90_in_sample + swap × nights` with a 1.5× margin | Netting the swap into `fwd_ret_8h` (contaminates every digest); ignoring it (it is larger than the round trip on any hold that crosses midnight) |
| **2026-09-08** | **`null_policy_carrier: d1_zscore_63d` — a D1 column, which is the opposite of the metals bot's choice** | Measured on the real matrix before any IC was read. The two-grid head start applies to the cross-asset partners (XAUUSD and the five FX majors serve D1 from 2014) but **not** to BTCUSD's own D1 tape, which starts 2018-02-09, the same week as its H4 tape — so a 315-D1-bar chain is the longest thing in the matrix rather than the shortest. First non-null slot: `slot_zscore_63` 2018-06-01 vs `d1_zscore_63d` 2018-12-22. Cutting on the slot column leaves four D1 columns 5–8 % null; cutting on the D1 column leaves NO null except the declared `usidx` family. Cost: 612 development slots, all in 2018 | `slot_zscore_63` (the analogue of what `exness_gold_sess` chose) — leaves five accidental null families instead of one declared one |
| **2026-09-08** | **`crypto` added to the shared `case_studies/utils/backtest_loaders.py::_cfd_pair_class`, with a regression test** | `BTCUSD` contains `"USD"`, so without the branch it resolved to `major_pairs` and a crypto universe would be priced at the FX majors' 0.6–1.3 bps — the same silent failure the `metals` branch was added to stop ("would have priced gold at 1.3 bps in silence"). Prefix-based, so no FX pair, metal or index changes class. `"8_hour": 1/3` added to `_CADENCE_CALENDAR_DAYS_PER_PERIOD` in the same edit — additive, no other case study declares that token, and without it the allocator warmup prefix would be read 4.5× too wide from the 1.5-day fallback. `bots/exness_btc_8h/tests/test_cfd_pair_class.py` (20 tests) asserts BTCUSD → `crypto` and that every symbol any other bot trades keeps the class it had | Declaring `major_pairs: [0.6, 1.3]` in this bot's `setup.yaml` (the silent mispricing); leaving `_cfd_pair_class` alone and letting `_normalize_cfd_costs` raise (loud, but leaves the trap for the next crypto bot) |
| **2026-09-08** | **The staleness screen in `05_evaluation` measures at the column's OWN grid** | Running the template's slot-resolution screen unchanged sent **28 of 73 columns to STOP for a reason that was arithmetic rather than empirical**: a D1 column joined onto a 3-slot-a-day grid cannot change more than once in three slots, so its slot-resolution staleness is exactly 2/3 = 0.667 by construction, above a 0.50 ceiling calibrated for a daily bot. The whole long-window state family, all three cross-asset families and the daily calendar flags failed before any of them had been measured against anything. The fix classifies each column by the grid `_features.py` builds it on — slot / daily / calendar — measures daily columns on one slot a day, and screens the three genuinely low-cadence calendar flags on **cardinality** instead. The 0.50 ceiling is unchanged everywhere it applies | Raising the ceiling to 0.70 (would hide a genuinely stale slot column); dropping the D1 families (throws away the two-grid design); leaving 28 columns at STOP (a wrong answer that looks like a result) |
| **2026-09-08** | **`plot_persistence` is not used in `03_financial_features`; a one-name figure replaces it** | Its right-hand panel is a cross-sectional rank correlation grouped by timestamp. On one instrument every group has one row, the Spearman is null, every column's stability is `nan`, and `np.nanmin` then makes the axis limit NaN — so it **raises** `Axis limits cannot be NaN or Inf` rather than returning a wrong number. The replacement is the feature's own autocorrelation on the decision grid plus the half-life read off it | Passing a fabricated second entity; catching the exception; skipping the section |
| **2026-09-08** | **No Kalman trend and no ARIMA in `04_model_based_features`** | Chosen from measurement, not preference: `01` found the 8-hour return's autocorrelation inside the white-noise band at every lag but one, and `bots/exness_fx_d1/BOT.md` open question 13 records that the same Kalman's observation-noise MLE collapsed to zero in every fold, making the emitted column a per-fold constant. A column that cannot say anything still costs a degree of freedom. What is fitted instead is a GARCH(1,1) and a two-state HMM, both of which describe the thing this instrument actually does | Copying all four estimators from the metals bot (two of them would emit near-constants); fitting nothing model-based |
| **2026-09-10** | **Hypothesis (B1), Kill criteria (B2) and DSR benchmark (B3) approved verbatim by the user**; phase 4 opened ("OPEN by user approval 2026-09-10, no fit yet") | The draft (`btc_8h_phase1_draft.md`, `ml4t-mentor`, arbitrated by `quant-orchestrator`) cleared the gate logged in `reviews.jsonl` 2026-09-10T06:37:47Z / T06:37:55Z. Operational threshold (a) drawdown = 8 % of allocated capital (user decision). R1 adopted as an EARLY-CLOSE GATE: 0/**356** (corrected same day from 86) HAC-significant prediction sets after BH at phase 4 closes the generation and skips the **2,136** (corrected from 516) phase-5 backtests | Opening phase 4 without a falsification line (the original 2026-09-08 block); leaving R1 a report-only clause (would let phase 5's backtests run even if phase 4 found nothing) |
| **2026-09-10** | **K5/K6 arithmetic corrected: 86→356 prediction sets, 516→2,136 backtest specs** | Coordinator flagged, before any stage was forked: the draft's "43 configs/label" counted declared CONFIGURATIONS, not PREDICTION SETS. Every `case_studies/config/lgb/*.yaml` GBM preset this bot's training menus resolve to declares `checkpoint_interval: 50, max_iterations: 500` (10 checkpoints), and `case_studies/research/model_planning.py::checkpoint_schedule` gives each checkpoint its own `prediction_hash` — so `07_gbm` publishes 15×10=150 prediction sets a label, not 15; `06_linear`'s 28 configs are unaffected (no checkpoint_interval on any linear preset). Corrected: 178 prediction sets/label, 356 phase-4 total, K5 = 178×2×3×2 = 2,136. Same arithmetic already measured on this account: `exness_gold_sess` phase 4 ("15 configs × 10 checkpoints × 2 labels = 300 sets"), `exness_fx_d1` generation-2 K=2,136 (`reviews.jsonl` 2026-09-10). B1/B2/B3's approved TEXT is left verbatim (dated correction notes added beside each occurrence); only the numbers the rule is evaluated against move | Silently fitting to the wrong K (would understate every DSR's trial-count penalty by ~4×); rewriting the approved blockquote in place (loses the audit trail of what the user actually approved) |
| **2026-09-10** | **Legacy V9 Continuum (magic 202500) STOPPED** — Windows tasks `V9_Continuum_DailyPnL` and `V9_Continuum_EnsureBot` disabled, the running process tree (`start_bot.bat` → `scratch/watchdog.py` → `v9_continuum.main`) terminated by the orchestrator, 0 open positions at the time. Demo login `Exness-MT5Trial7` equity read at 891.15 USD (down from 10,000 USD on 2026-09-05, drawn down by the legacy bot); user decided to **reset the demo to 10,000 USD** in the Exness Personal Area — **pending user action** | Closes Open question 1 (legacy bot live) as of today; does not retroactively un-contaminate the holdout window (2025-09-01 → 2026-08-31), which the legacy bot traded through before being stopped. Capital-viability note (Open question 2, min order ≈ 88 % of equity) becomes **pending demo reset** until the reset happens | Leaving the legacy bot running through phase 4-7 development (rejected by the user 2026-09-10); deleting the legacy reference copy (kept per the 2026-09-05 decision, read-only) |
| **2026-09-10** | **K6 vol-target arms declared**: n = 3 (`vol_target_20`/`_40`/`_60`, target annualized vol 0.20/0.40/0.60, `vol_lookback_slots: 63`), added to `setup.yaml::backtest.sweep.position_sizing.vol_target_arms` | K6 counting rule (`bots/exness_gold_sess/PHASE567_DECLARATION.md:266`) requires n to be listed in `setup.yaml` before the first fit; declared here since `setup.yaml` carried a bare `method: volatility_target` with no arm list. `vol_lookback_slots: 63` reuses an already-declared feature window (`features.windows.close_to_close_volatility`, `garman_klass`) rather than inventing a new one; 0.20/0.40/0.60 brackets the asset's own realised volatility scale without repeating the discarded boundary-breach number (Rule 1, `reviews.jsonl` 2026-09-10T06:37:55Z) | Leaving n undeclared (blocks K6 from ever being computable); reusing the risk-controls percentages (3/5/10 %, calibrated for a stop distance, not a target vol) |
| **2026-09-10** | **`06_linear.py`, `07_gbm.py`, `12_model_analysis.py`, `13_backtest.py` forked from `exness_gold_sess`** (paired `.ipynb`s generated, `scripts/sync_notebooks.py --check` 0 drift on all four); `08`-`11` NOT forked because `exness_gold_sess` itself does not have them (confirmed: `06`, `07`, `12`, `13` only, no `08_tabular_dl`/`09_dl_lstm`/etc. on any Exness-fleet sibling) | Coordinator instruction: fork 06/07/13 "and whatever `exness_gold_sess` uses between them that this bot lacks". Checked the actual directory rather than assuming a gap exists | Forking `08`-`11` from a DIFFERENT case study (e.g. `etfs`) that does have them — would add undeclared trial families no menu here lists |
| **2026-09-10** | **`session_book_of` added to `_features.py`; `outcome_column_for` added to `_model_reading.py`** | `06`/`07`/`12`/`13` need a book resolver (gold's `session_of` returns `london`/`ny`; this bot's grid has no venue, so the resolver reads the swap clock instead: `no_swap_night` = {00:00, 08:00} UTC, `us_hours` = {16:00}, a true two-way partition — `"all"` is never returned, it is the union, matching `prediction_staleness_by_book`'s own `__pooled__` bucket). `outcome_column_for` is ported unchanged from the gold sibling for forward compatibility with a future classification label, even though this generation's `classification_eval_label` is empty and every call returns `"actual"` | Reusing gold's `session_of` directly (wrong domain: venue sessions, not a swap clock); inlining the outcome-column rule into each of 4 stages separately (drift risk) |
| **2026-09-10** | **R1 wired declaratively**: `setup.yaml::kill_criteria` block (`r1_early_close: true`, `r1_ic_star_validation: 0.1144`, `r1_fdr_alpha: 0.05`, `r1_required_sign_consistency: 1.0`, `r1_expected_prediction_sets: 356`), read by `12_model_analysis.py`, which applies Benjamini-Hochberg (`ml4t.diagnostic.evaluation.stats.benjamini_hochberg_fdr`, the same function `05_evaluation.py` already uses) across the pooled OOF HAC p-values of the assembled population, writes `evaluation/r1_early_close_verdict.json`, and RAISES before its own handoff section (and `13_backtest.py` independently re-reads the same artifact and raises too) if zero prediction sets clear BH-significance AND 4/4-fold sign consistency | Coordinator instruction #2: "the rule declarative in setup.yaml..., not hard-coded". A prediction set "clears R1" only on BOTH conditions — BH-significant alone would pass a configuration whose folds disagree, which is not "0.36× IC\*, 0/73 BH" language B1 was written against | Hard-coding 0.1144/0.05/1.0 as Python literals in `12_model_analysis.py` (what the first draft of this fork did, caught by `test_12_model_analysis_reads_the_flag_not_a_literal`); making R1 a warning rather than a `raise` (would let `13_backtest` run after a closed generation without a human noticing) |
| **2026-09-10** | **`13_backtest.py` written SIMPLER than `exness_gold_sess`'s**: no H1-grid hold derivation (`HOLD_BARS[label] = labels.rebalance_step[label]`, the same for every book), all three session books ENGINE-run with no arithmetic pooled sleeve (`all` is the untouched grid, not a 50/50 sum of two others) | `setup.yaml::backtest.price_grid_expresses_primary_label: true` already makes the hold trivial — entry and exit ARE adjacent grid rows — and this bot's `all` book is a real single-venue grid, unlike the gold sibling's two-venue `london`/`ny` pair that cannot coexist in one broker position. Porting gold's `_hold.py` (H1-tape hold derivation) and its arithmetic-pooled-sleeve bookkeeping would add code for a problem this bot does not have | Porting `_hold.py`/`_report_phase5.py`/`_screens.py` unchanged (gold-specific complexity with no BTC counterpart); declaring a `pooled_book` key in `setup.yaml` to match gold's shape (would be a fabricated arithmetic sleeve with nothing to sum) |
| **2026-09-10** | **Two shared-library blockers found and NOT fixed**: `backtest_runner.py::apply_session_filter` hard-coded to `exness_gold_sess`; `backtest_loaders.py::_PRICE_CONFIG` has no `exness_btc_8h` entry (open question 5 already knew the second; the first is new). Diffs for both PROPOSED in Open question 5, neither applied | Hard constraint: no edits to `case_studies/utils/*` without the mentor's review; "propose the diff, do not apply". `13_backtest.py` is forked and readable with these calls exactly as they will run once the diffs land, rather than stubbed around them | Editing `backtest_runner.py`/`backtest_loaders.py` directly (forbidden without review); writing `13_backtest.py` around the blockers with a different, bot-local session filter and price loader (would diverge from the shared engine every other Ch16-19 stage uses, and duplicate code the diff removes the need for) |
| **2026-09-10** | **`tests/overrides.yaml` extended** (4 new entries, `case_studies/exness_btc_8h/{06_linear,07_gbm,12_model_analysis,13_backtest}`, all `skip: true` with a reason); **`bots/exness_btc_8h/tests/test_phase4_declarations.py` added** (16 static, offline tests: K5 derivable from `setup.yaml` + `config/training/*.yaml` + the shared GBM presets' `checkpoint_interval`/`max_iterations` alone; R1 declarative not hard-coded; every forked stage guards against the holdout and carries no hard-coded date literal in code on or after `holdout_start`) | Coordinator instruction #3. The K5-derivability test is what caught nothing new post-correction (it PASSES against the corrected 2,136), but it is what would have caught the original 86/516 error had it existed before the draft was approved — written now so the next generation's arithmetic is checked the same way | A test that reads the registry or an MT5 fixture (would violate "no fit, no run" for this task); asserting K5 == 516 (the pre-correction number) |
| **2026-09-10** | **Synced to `~/ml4t/case_studies/exness_btc_8h/` and `~/ml4t/bots/exness_btc_8h/`** via `rsync -av` (no `--delete`, `pyproject.toml` excluded, `legacy_v9_continuum/` excluded from the bot-dir sync); `tests/overrides.yaml`'s 4 new entries inserted SURGICALLY into the WSL2 clone's own uncommitted copy (line-range extraction + `sed` insert at the matching anchor line) rather than overwritten, because that file already carried OTHER bots' unrelated uncommitted work in that clone. `bots/README.md` and `bots/assets/BTCUSD.md` (edited in an earlier task today) were deliberately NOT synced — out of this task's scope and a second file each bot does not own alone | The WSL2 clone (`~/ml4t`, origin `/mnt/d/05_Quant/machine-learning-for-trading`) is a SEPARATE git working tree shared by every bot-builder session on this host; a wholesale copy of a shared file would have clobbered concurrent work | `rsync --delete` (would have deleted whatever other in-flight work exists under those trees that is not on the Windows side yet); overwriting the whole `tests/overrides.yaml` file (would have discarded other bots' uncommitted entries) |
| **2026-09-10** | **`06_linear` fit attempt, BLOCKED** — coordinator approved running the phase-4 fit now (WSL2 idle, claimed); ran `06_linear.py` via papermill (`WORKSPACE=/home/pro_trader/ml4t/experiments/exness_btc_8h`, `ML4T_OUTPUT_DIR` same, `LD_LIBRARY_PATH=/home/pro_trader/omp`). FAILED after 12.3 s, cell 14, `ValueError: linear runner requires timestamp and an entity key`, inside `utils/modeling.py::load_modeling_dataset` (~line 924: `entity_cols = [c for c in entity_cols if features[c].n_unique() > 1]` strips `symbol` because it is constant on a ONE-instrument panel, leaving `entity_cols == []`). Stopped immediately per the runner's own gate (`_load_batch_base`, `case_studies/utils/linear.py:544`); `07_gbm.py` and `12_model_analysis.py` were NOT attempted (the wrapper script only proceeds past a zero exit code); `13_backtest.py` was never attempted (excluded by instruction regardless of outcome). ALSO found, independently: `exness_fx_d1`'s `13_backtest` (`13_backtest_gen3_cross_sectional.ipynb`, PID 822) was ACTIVELY RUNNING in WSL2 at both the start and the end of this attempt, contradicting the "WSL2 is idle" premise the run was approved under — this alone would have required waiting per the coordinator's own instruction, independent of the shared-library blocker. Proposed diff for `utils/modeling.py` in Open questions item 14, NOT applied. Zero training runs registered; `~/ml4t/experiments/exness_btc_8h/exness_btc_8h/run_log/registry.db` now exists (created empty by `Study.open`'s adopt-path) but carries 0 rows in every table — the only side effect of the attempt. Full log `~/ml4t/btc8h_phase4.log` | The failure is a real, load-bearing gap: `utils/modeling.py::load_modeling_dataset` is "the canonical data loading function for ALL Ch11+ modeling notebooks" (its own docstring) and this bot is the FIRST one-instrument case study in the Exness fleet to reach it (`exness_gold_sess` 2 instruments, `exness_fx_d1` 5, `exness_usidx_sess` 2) — the constant-entity-column filter was written for a cross-sectional panel losing an instrument to a data gap, not for a panel that is one instrument BY DESIGN. Not a workaround-able bug from this bot's own files: `resolve_model_request` → `_load_batch_base` → `load_modeling_dataset` has no caller-side parameter to preserve a constant entity column | Editing `utils/modeling.py` directly (a shared, top-level, extremely high-blast-radius module — same conservative policy as `case_studies/utils/*`, and doubly warranted while another agent's process was actively running against the same shared codebase); retrying the run immediately without a fix (would fail identically); inventing a bot-local monkeypatch of a shared function (hides the defect from every other single-instrument bot that will hit it later) |
| **2026-09-10** | **Fleet mentor gate on the three proposed diffs (#4 `apply_session_filter`, #5 `_PRICE_CONFIG`, #6 `utils/modeling.py`): GAP / GAP / NOT_EVIDENCE_YET.** Mentor findings, verbatim: #4 real bug — `"all"` filters to 0 rows and raises (`session_book_of` never returns it); #5 wrong schema — the proposal used `symbols`/`timeframe`/`close_keyed`, the real schema is `entity_col`/`time_col`/`close_col`/`ohlcv`/`loader`/`drop_cols` (+ `price_lineage`) plus a loader dispatch branch; #6 wrong mechanism — emptiness-driven would silently break the `max_symbols=1` preview guard on `etfs`/`fx_pairs`, must be DECLARATION-driven. `case_studies/utils/` and `utils/` held SERIALIZED (single-writer) while `exness_fx_d1`'s builder edited them (lot-sizing / fractional-lot work, gate item #3). Required tests written first, verbatim per the mentor's three five/five/six-item lists, in `bots/exness_btc_8h/tests/test_session_filter_diff.py`, `test_price_config_diff.py`, `test_entity_cols_diff.py` (skip-gated on a live probe of the not-yet-landed behaviour, so they activate automatically once the diff lands — no manual un-skipping); `13_backtest.py` fixed to pass `session_filter: "all"` explicitly. All three rewritten diffs held pending "go" | A gate is not a rejection: every finding pointed at a fixable gap in the proposal, not a wrong problem statement. Writing the tests before the diffs land is what "ready to run the moment it lands" means literally, and catching a real PIT leak (item 5, see below) while writing test 5.3 vindicates writing behavioural tests against real data rather than reviewing the diff text alone | Applying the diffs before the gate (skips the mentor's review entirely); rewriting the diffs from the gate findings alone without also encoding them as tests (the gate's own findings would not be provable to have been addressed) |
| **2026-09-10** | **"GO" received; all three diffs APPLIED on the Windows tree** (`case_studies/utils/backtest_runner.py`, `case_studies/utils/backtest_loaders.py`, `utils/modeling.py`) after `exness_fx_d1`'s builder released the serialization lock. Respected the three do-not-touch notes: `read_mt5_lot_sizing_declaration` (`backtest_loaders.py`, fx_d1's new function) untouched; `_apply_lot_floor_if_declared` (`backtest_runner.py`) and `backtest_presets.py` untouched; `exness_gold_sess`'s uncommitted `classification_eval_label` diff in `utils/modeling.py` left intact — only the entity-column block was edited. NOT synced to `~/ml4t` (sweep still running there, per instruction); NOT run `06`/`07`/`12` (per instruction) | Verified rather than assumed: `bots/exness_btc_8h/tests` full suite **98 passed, 4 skipped** (skips are `fx_pairs`/`etfs` data not built in this sandbox, not the diffs) — every gate item's skip-gated test flipped to a real pass the moment its diff landed, with zero code changes to the tests themselves. `bots/exness_fx_d1/tests/test_lot_floor_currency.py` **22 passed** proves nothing of `exness_fx_d1`'s concurrent work broke. `experiments/xau_fx_mt5/tests/test_execution_guards.py` run against the same Windows-tree edits (see below) | Skipping the "nothing else broke" proof and reporting only this bot's own green suite (would not catch a collision with fx_d1's or xau_fx_mt5's concurrent, unrelated shared-code edits) |
| **2026-09-10** | **Combined re-gate: #4/#5/#6 MET, four materials applied.** #4 and #5 MET outright (the PIT leak found while testing #5 confirmed real and correctly fixed); #6's blast radius proven zero by static check (only `exness_btc_8h` declares `universe.n_assets: 1`), full MET pending `TestBtcEntityColsAndFoldGateItem3` passing in WSL2 — `exness_fx_d1`'s builder does that sync and run after their sweep logs DONE, not this bot's writer. Four materials, all on files this bot's builder owns the edit region of: **(1)** `decision.session_filter_all_value` declared in `setup.yaml`, `apply_session_filter` reads it instead of hard-coding `case_study == "exness_btc_8h" and book == "all"`; **(2)** `utils/modeling.py` asserts `n_assets == len(symbols)` when the single-asset branch fires, raises on the two declarations disagreeing; **(3)** `_naive_end_date_string` added to `backtest_loaders.py`, coerces `end_date` (date/str/tz-aware datetime) ONCE before both the `load_mt5_bars` call and the re-window clip - catching a REAL `SchemaError` the previous version would have raised on a non-string `end_date`, found by the very test the material demanded; **(4)** `test_entity_cols_diff.py`'s skip probe rewritten tri-state (`fixed`/`missing_data`/`not_fixed`) so a reverted diff FAILS instead of hiding behind a "no data" skip. Native run (repo copy, no `ML4T_OUTPUT_DIR`, no sync): **100 passed, 15 skipped, 0 failed** | Material #3 in particular vindicates writing the required tests before declaring a material "done": the first version of the PIT-clip fix (applied for the earlier re-gate) looked complete by inspection and only failed once a non-string `end_date` was actually tried, exactly the class of bug a "test for each input type" exists to catch | Reporting the diffs fixed without re-running every gated test (would have shipped the `SchemaError` regression); setting `ML4T_OUTPUT_DIR` to the stale, un-synced experiment workspace for this native run (masks the `session_filter_all_value` KeyError behind a *different* wrong config rather than exercising the real repo declaration) |
| **2026-09-08** | **`bots/exness_btc_8h/__init__.py` added** | Without it pytest resolves this bot's `tests/test_data_quality.py` to the top-level package name `tests`, colliding with the repository's own `tests/` package and with the identically named module in every sibling bot: adding this bot's suite made `exness_gold_sess` fail collection under a whole-tree `pytest bots`. `exness_fx_d1` already has one for the same reason | Renaming this bot's test modules (hides the shared defect); leaving `pytest bots` broken |
| **2026-09-10** | **Combined re-gate's four materials RE-VERIFIED against the WSL2/`ML4T_OUTPUT_DIR` invocation the orchestrator specified** (`cd /mnt/d/.../machine-learning-for-trading && ... ML4T_OUTPUT_DIR=/home/pro_trader/ml4t/experiments/exness_btc_8h uv run --project /home/pro_trader/ml4t --no-sync --with pytest==9.0.3 python -m pytest bots/exness_btc_8h/tests -q -p no:cacheprovider -rs`), reconciling with row 200's NATIVE (no `ML4T_OUTPUT_DIR`) numbers. **Locations** — (1) `case_studies/exness_btc_8h/config/setup.yaml:145` (`session_filter_all_value: all`) read by `case_studies/utils/backtest_runner.py:886`; (2) `utils/modeling.py:960-967` (`n_assets == len(symbols)` assert, only checked when the single-asset branch fires); (3) `case_studies/utils/backtest_loaders.py:891-915` (`_naive_end_date_string`), applied once at `:1140` before `load_mt5_bars` and again at `:1178-1183` for the PIT re-window clip; (4) `bots/exness_btc_8h/tests/test_entity_cols_diff.py:40-64` (`_entity_cols_state_for_btc`, three-state), gating decorators at `:126`, `:158`, `:241` (`SKIP_ON_MISSING_DATA` set at `:122`). **This run (WSL2, real built labels/features, STALE experiment config): 4 failed, 107 passed, 4 skipped**, reproduced identically 3×. All 4 failures are `test_session_filter_diff.py::TestAllValueIsDeclaredNotHardCodedMaterial1` (both tests) plus the two tests it protects (`TestBtcAllIsAnExplicitNoOpItem3`, `TestThreeBooksPairwiseDifferentItem5`) — every one a `KeyError: 'session_filter_all_value'` or the resulting empty-frame `ValueError`. **Root cause is NOT the material**: `case_studies/exness_btc_8h/config/setup.yaml:145` on the Windows tree carries the key (confirmed by direct read); `ML4T_OUTPUT_DIR` redirects config reads to `~/ml4t/experiments/exness_btc_8h/exness_btc_8h/config/setup.yaml`, a snapshot `create_experiment.py` took 2026-09-08 and last refreshed 2026-09-10T21:05 local — BEFORE material (1) landed (~22:41-22:52 local) — and the separately-rsynced mirror `~/ml4t/case_studies/exness_btc_8h/config/setup.yaml` is missing the same key for the same reason. Confirmed the OTHER three materials are unaffected by this staleness (their code lives in shared files already current in WSL2, not per-bot config): `test_price_config_diff.py` 19/19 (all pass, including all 4 `end_date` types in `TestEndDateCoercionMaterial3`), `test_entity_cols_diff.py` 9/9 non-skipped. **Reconciled with row 200's native run** (100 passed, 15 skipped, 0 failed, independently re-reproduced here too — `test_session_filter_diff.py` 20/20, `test_price_config_diff.py` 18 passed/1 skipped, `test_entity_cols_diff.py` 4 passed/11 skipped, matching row 200 exactly): the native run reads the up-to-date Windows-tree config directly (no failures) but cannot see the built labels/features (only ever written to the WSL2 experiment fork), hence more skips; this run reads the built data but a stale config snapshot. Neither run contradicts the other — together they show all four materials are correctly implemented on the Windows tree and the ONE reproducible gap is an unsynced WSL2 config artifact. **Also directly investigated and closed the mid-task `SchemaError` report** (`bots/_shared/mt5_loader.py:694`, `invalid series dtype: expected String, got datetime[μs, UTC]`): reproduced it, but ONLY by calling `load_mt5_bars` directly with a raw tz-aware `datetime` `end_date`, bypassing the coercion `backtest_loaders.py:1140` already applies before that call (this is exactly the bug row 200 says material (3) already fixed). Grepped all 7 files under `bots/exness_btc_8h/tests/` for `load_mt5_bars`/`end_date` usage: every call site passes a string; none reaches `mt5_loader.py` un-coerced. `load_mt5_bars` itself remains fragile to a non-string `end_date`/`start_date` for any OTHER direct caller — flagged, not fixed (a shared, fleet-wide file outside the three already-edited blocks, no test in this gate needs it changed). **Nothing synced to `~/ml4t`, no stage run** — `exness_fx_d1`'s gen-3 sweep (PID 822) confirmed still alive at both the start and end of this verification | The orchestrator's mid-task course-correction asked specifically for the exact given invocation's outcome, not just a clean substitute; running it for real (rather than trusting row 200's native-only account) is what surfaced the WSL2 config-staleness gap and let it be attributed correctly (environment, not material) rather than left unexplained | Silently substituting the native run for the mandated WSL2 one (would leave the orchestrator's specific question unanswered); patching `~/ml4t`'s stale config to force a clean WSL2 run (violates the explicit no-sync-while-fx_d1-runs rule); hardening `bots/_shared/mt5_loader.py` unprompted (fleet-wide file, no test in scope needs it, another bot's builder may depend on its current shape) |
| **2026-09-11** | **Three user decisions recorded** (`reviews.jsonl` 2026-09-10T22:10:25Z / UTC 2026-09-10T22:10Z, `quant-orchestrator`, on the mentor's `GEN2_DECISION_DRAFT_2026-09-11.md` package): **Q1 (M2) = option B** — narrow the code to the pre-registered B2 text (BH alone gates R1; sign consistency is REPORTED, not gating). **Q2 = generation 1 CLOSED, generation 2 CONDITIONAL** (not "retired", no new D1 bot opened). **Q3 = on-chain data source: NOT NOW** (no vendor/key/backfill approved today; condition (1) of generation 2 stays open). **M2 code change applied**: `case_studies/exness_btc_8h/12_model_analysis.py:374-382` — `clears_r1` narrowed from `pl.col("bh_significant") & (pl.col("sign_consistency") >= r1_min_sign_consistency)` to `pl.col("bh_significant").alias("clears_r1")` (BH alone); markdown cell `:336-350` and both raise/print strings (`:414-421`, `:429-437`) reworded to match; verdict JSON (`:393-410`) renames `required_sign_consistency` → `sign_consistency_reported_threshold`, adds `gate_condition: "bh_fdr_only"` and `n_sign_consistent_4of4_reported`; `setup.yaml:901-905` (`r1_required_sign_consistency` comment reworded "reported, NOT gating"); `setup.yaml:729-738` (`on-chain and flow` `inputs:` comment corrected — DefiLlama/CoinGecko and Binance funding/premium ARE wired key-free, none supplies netflow/miner reserves/ETF creations, none has vintage) and `BOT.md`, Phase status row 0, reworded to match. `.py`→`.ipynb` re-paired with `uv run --with jupytext python scripts/sync_notebooks.py case_studies/exness_btc_8h/12_model_analysis.py` in WSL2 (0 drift confirmed with `--check` before and after), never hand-edited. New static test `test_clears_r1_gates_on_bh_alone_not_sign_consistency` added to `bots/exness_btc_8h/tests/test_phase4_declarations.py`, greps the gate cell for the narrowed `clears_r1` expression and asserts the old AND-form is absent. **Proof no result moved**: WSL2 idle confirmed (`ps aux \| grep papermill`, no real process, before and after); `case_studies/exness_btc_8h/` and `bots/exness_btc_8h/` synced to `~/ml4t` (`rsync -av`, no `--delete`, `__pycache__` excluded); stage 12 re-run ALONE (`~/btc_scripts/run_stage.py 12_model_analysis`, `LD_LIBRARY_PATH=/home/pro_trader/omp`, `ML4T_OUTPUT_DIR=~/ml4t/experiments/exness_btc_8h`), log `~/ml4t/btc8h_phase4_m2_rerun.log`: `RuntimeError` raised again by design after 62.8s at cell 17, "0 of 356 prediction sets clear BH p < 0.05 ... 10/356 are sign-consistent 4/4 folds, reported only". Verdict JSON byte-identical on every outcome number: `n_prediction_sets: 356, n_bh_significant: 0, n_clears_r1: 0, best_ic_mean: 0.030302525019830308, best_config: "default_mse", generation_closed: true`, plus new keys `gate_condition: "bh_fdr_only", sign_consistency_reported_threshold: 1.0, n_sign_consistent_4of4_reported: 10`. Registry unchanged before/after (`load_training_runs`/`load_prediction_sets`/`load_backtest_runs`): **86 training runs, 356 prediction sets, 0 backtests**; no `backtest_runs`/`backtests` directory exists — `06`/`07`/`13` did NOT run, holdout not read. **Test counts**: full bot suite from WSL2 against the Windows tree (`ML4T_OUTPUT_DIR=/home/pro_trader/ml4t/experiments/exness_btc_8h uv run --project /home/pro_trader/ml4t --no-sync --with pytest==9.0.3 python -m pytest bots/exness_btc_8h/tests -q -p no:cacheprovider -rs`): **112 passed, 4 skipped** (was 111 passed, 4 skipped before this task's one new test; the 4 skips are the same `fx_pairs`/`etfs` "not built in this environment" skips as every prior run, not new failures) | M2 was a real discrepancy between the user-approved B2 text (BH alone) and the shipped code (BH AND sign 4/4); the mentor's own recommendation (§5 of the draft) was option B because tightening a pre-registered rule after seeing generation-1 results is the same "threshold change made after seeing results" pattern flagged on `xau_fx_mt5` (`bots/xau_fx_mt5/BOT.md:885`) even when the tightening direction is stricter. Re-running stage 12 alone (not 06/07/13) is the minimal proof that a wording/gate-logic fix changes no fitted result: the same 86 training runs and 356 prediction sets feed the same R1 computation, only the pass/fail rule read fewer columns | Option A (amend B2's text to add the sign condition) — rejected by the user, matches the mentor's ranking (a post-hoc tightening is a bigger provenance problem than narrowing code to a rule that was already approved); recording generation 2 as "retired" (Q2) — rejected, the `on-chain and flow` path is a real, costed, un-taken option, not a dead end; approving on-chain data sourcing today (Q3) — rejected, no vendor/key/backfill/vintage decision was made this session |

## Trials counted for the Deflated Sharpe Ratio

| Date | What was tried (models, labels, sessions, presets) | Count |
|---|---|---|
| 2026-09-08 | Phase 3 declared a feature register in `setup.yaml::features.families` **before any information coefficient was read**, and `05_evaluation` scored all 73 columns on the validation folds. A univariate screen is not a selection: nothing was fitted, nothing was chosen, `run_log/` is empty and no `registry.db` exists | **0** |
| — | Declared and NOT run, so the reader knows what phase 5 will cost when it opens: 178 prediction sets a label (28 linear, single checkpoint each; 15 GBM × 10 checkpoints each — `checkpoint_interval: 50, max_iterations: 500` on every `case_studies/config/lgb/*.yaml` preset this menu resolves to) × 2 labels × 3 session books × 2 signal specs = **K5 = 2,136** backtest specs. **CORRECTED 2026-09-10** from a first-pass 516 that used the 43 declared *configurations* rather than the 178 *prediction sets* a GBM checkpoint schedule actually publishes (`case_studies/research/model_planning.py::checkpoint_schedule`) — caught by the coordinator before `06`/`07`/`12`/`13` were forked, so nothing was fitted under the wrong count. `member_digest`: **placeholder — to be filled when the `13` sweep runs** (`13_backtest`, forked 2026-09-10, not run). The Deflated Sharpe of anything phase 5 produces divides by 2,136, and it rises with every family later added (`on-chain and flow`, a triple-barrier label, a `short_only` pass) | 0 so far |
| 2026-09-10 | **K6 counting rule declared** (not run): K6 = K5 (2,136) + survivors × 2 labels × 8 risk controls (`setup.yaml::backtest.sweep.risk_controls.position`) × n = 3 vol-target arms (`setup.yaml::backtest.sweep.position_sizing.vol_target_arms`, declared today). Convention: risk overlays are trials (fleet rule, `bots/exness_gold_sess/PHASE567_DECLARATION.md:266` — `resolve_solvent_carrier` ranks signal/allocation/risk_overlay together, so they are candidates and therefore trials); the cost grid (`cost_grid_bps`, 13 points) is **not** a trial. `member_digest` for K6 is also a placeholder until phase 5-6 runs | 0 so far |
| 2026-09-10 | **Phase 4 stages forked** (`06_linear.py`, `07_gbm.py`, `12_model_analysis.py`, `13_backtest.py` from `exness_gold_sess`, adapted for one instrument / no cross-section / 8h grid / PPY 365 / three books / two signal specs), R1 wired declaratively in `setup.yaml::kill_criteria.r1_early_close`, 16 static tests added (`bots/exness_btc_8h/tests/test_phase4_declarations.py`), synced to `~/ml4t`, full bot suite green in WSL2 (61 passed with the built experiment artifacts). **Forking is not fitting**: no stage executed, no `06`/`07`/`12`/`13` run, `run_log/` empty in both `case_studies/exness_btc_8h/` and the synced `~/ml4t` copy, `registry.db` absent, this row moves no K. Two shared-library blockers found (`apply_session_filter`, `_PRICE_CONFIG`), diffs proposed and NOT applied (Open question 5) | **0** |
| 2026-09-10 | **`06_linear.py` fit ATTEMPTED, FAILED before any fold fit** (coordinator-approved run; see Phase status row 4 and Decisions log "06_linear fit attempt, blocked"). `resolve()` raised inside `utils/modeling.py::load_modeling_dataset` (a one-instrument-panel defect in a SHARED function, Open question 14) before `run_model_population` was ever called. **0 training runs, 0 prediction sets registered** — `~/ml4t/experiments/exness_btc_8h/exness_btc_8h/run_log/registry.db` now exists (created empty by the study-open step) but every table is empty; verified by reading the registry after the attempt. `07_gbm.py`, `12_model_analysis.py` (and therefore the R1 verdict) and `13_backtest.py` were NOT run. This row is an attempted-and-failed fit, not a completed one: it counts as **0** toward K by the same rule an incomplete backtest counts as 0 elsewhere in this repository — nothing here is a candidate a Deflated Sharpe Ratio could divide by, because nothing produced a Sharpe, an IC, or even a completed training identity | **0** |
| 2026-09-10 | **Three shared-library diffs (Open questions 5, 14) went through the fleet mentor gate, were rewritten per its findings, and were APPLIED** on `case_studies/utils/backtest_runner.py`, `case_studies/utils/backtest_loaders.py` and `utils/modeling.py`, after `exness_fx_d1`'s builder released the serialization lock. **No fit, no fold, no training run, no prediction set — infrastructure only.** `06_linear`/`07_gbm`/`12_model_analysis`/`13_backtest` were NOT run this round (explicit instruction). Verified with unit tests, not by inspection: `bots/exness_btc_8h/tests` 98 passed / 4 skipped, `bots/exness_fx_d1/tests/test_lot_floor_currency.py` 22 passed, `experiments/xau_fx_mt5/tests/test_execution_guards.py` run against the same edits (see report). This row moves no K: fixing a shared library is not a trial any more than forking a stage was | **0** |
| 2026-09-10 | **Four non-blocking MATERIALS from the combined re-gate verified** (session-filter-all-value declaration, n_assets/symbols assert, `end_date` coercion, tri-state skip probe) — re-run against both the WSL2/`ML4T_OUTPUT_DIR` invocation (4 failed / 107 passed / 4 skipped, failures attributed to an unsynced WSL2 config snapshot, not the materials) and the native repo-copy invocation (100 passed / 15 skipped / 0 failed, reconciled and independently reproduced). **No fit, no fold, no training run, no prediction set, no sync to `~/ml4t`, no stage run** — test execution and source-code inspection only. This row moves no K | **0** |
| 2026-09-10 | **THE PHASE-4 FIT RAN — `06_linear` (56 training runs, 56 prediction sets) → `07_gbm` (30 training runs, 300 prediction sets) → `12_model_analysis`, R1 over the complete 356-prediction-set population.** Coordinator-approved, sequential, WSL2, background, `~/ml4t/btc8h_phase4.log`, 2026-09-10T17:17:31Z–17:25:50Z (8m19s). **R1 (`roadmap.md:297`: phase 4, a REPORT and the adopted early-close gate — NOT a Sharpe-based selection): 0/356 prediction sets clear BH p<0.05 AND sign_consistency=1.0 (0/356 BH-significant alone).** Best pooled IC anywhere: `default_mse` (GBM) on `fwd_ret_8h`, +0.0303, 26.49 % of IC*=0.1144 (validation). Per label/family best: `fwd_ret_8h` gbm +0.0303 / linear −0.0017; `fwd_ret_24h` gbm +0.0120 / linear −0.0266 — every one far below IC*, none BH-significant. **Generation CLOSED**: `kill_criteria.r1_early_close: true` fired, `13_backtest` (K5 = 2,136) does **NOT** open this generation; verdict persisted to `evaluation/r1_early_close_verdict.json`. Registry confirms 86 training runs / 356 prediction sets, matching the corrected K5 derivation with zero drift; `13_backtest` was not run, not attempted (no `backtest_runs`/`backtests` in `run_log/`). **This row counts 0 toward the Deflated Sharpe Ratio's K**, by the same rule phase 3's 73-column feature screen counted 0: R1 is an IC-level report/gate, not the Sharpe-based selection the DSR is computed against (`13_backtest`/`19_strategy_analysis`), and no backtest ran. K5 = 2,136 remains a declared-but-undrawn number: it never materialises for a closed generation | **0** |
| 2026-09-10 | **Mentor gate on the phase-4 closure: MET.** `ml4t-mentor` (opus, read-only, artifacts read directly in `~/ml4t/experiments/exness_btc_8h`) confirmed every number byte-for-byte against the verdict JSON and the registry, confirmed R1 ran exactly as pre-declared (BH step-up over 356 p-values, sign consistency from `_model_reading.py`, thresholds read from `setup.yaml::kill_criteria`, test-locked), and confirmed the holdout guards printed "untouched" in all three notebooks. **Material M1 applied and verified same day**: `12_model_analysis.py` now persists the complete 356-row diagnostics frame to `evaluation/phase4_ic_diagnostics.parquet` (+`.csv`) immediately before the R1 cell (module docstring "What it writes" updated to match); stage synced to `~/ml4t` (sha256-identical) and RE-RUN — stage 12 ONLY, nothing else — 118.5 s, R1 raised again by design (`generation_closed: true`). Byte-identical to the first run: `n_prediction_sets: 356`, `best_ic_mean: 0.030302525019830308`. Registry unchanged: 86 training runs, 356 prediction sets, **0 backtests**. **GBM K-independent numbers, from the persisted frame** (300 GBM rows, 150/label): max `|hac_t|` overall **1.9599** (`fwd_ret_8h`, `default_mse`, checkpoint 150 — the same leader as the best-IC row, `hac_p` 0.0501), just short of the unadjusted 1.96 threshold; **0/300 GBM sets clear `|hac_t| >= 1.96` unadjusted**, before any BH correction. Per label: `fwd_ret_8h` max `|t|` 1.9599 (0 clearing of 150); `fwd_ret_24h` max `|t|` 0.8160 (0 clearing of 150, far weaker). Combined with the mentor's own linear-side reading (56/56 sets negative IC both labels, max `|t|` 1.937 on `fwd_ret_24h lasso_f0.85`, `p` 0.0529, 0/56 clearing): **0 of all 356 prediction sets clear `|hac_t| >= 1.96` unadjusted, before BH is even applied** — the BH step and the sign-consistency condition (M2) never had a candidate to act on. **Material M2 recorded** (dated note next to R1 in §Kill criteria above): the code's closure condition (BH AND sign 4/4) is broader than the approved B2 text (BH alone); identical outcome today; must be reconciled before generation 2. **Cross-bot upgraded** (see §Hypothesis): family `dollar` is now "scored at phase 3 AND spent inside this closed phase-4 population", not merely "scored" | **0** |
| 2026-09-11 | **M2 option B applied; stage 12 re-run only.** `clears_r1` narrowed to BH significance alone (`12_model_analysis.py:374-382`); sign consistency reported, not gating. `06_linear`/`07_gbm`/`13_backtest` NOT re-run — only `12_model_analysis` (`~/btc_scripts/run_stage.py 12_model_analysis`) re-executed against the SAME 86 training runs / 356 prediction sets already in the registry. Verdict unchanged on every outcome number (`n_prediction_sets: 356, n_bh_significant: 0, n_clears_r1: 0, best_ic_mean: 0.030302525019830308, generation_closed: true`); registry unchanged (86/356/0). No fit, no fold, no new training run, no new prediction set — a rule-wording fix, not a trial | **K = 0; FDR-n unchanged (73 then 356)** |

**Cumulative K = 0.** No Sharpe-based selection has been made for this bot: `06_linear`/`07_gbm` fit 86 real models over 356 prediction sets and `12_model_analysis`'s R1 gate read them and CLOSED the generation before `13_backtest` (the Sharpe-based selection stage the DSR's K is computed against) ever opened — by the same convention phase 3's IC screen used, an IC-level report/gate is not itself a trial the Deflated Sharpe Ratio divides by. K5 = 2,136 was declared in advance and never materialises now that the generation is closed. Today's writes (Hypothesis, Kill criteria, DSR benchmark, K5/K6 corrections, phase-4 stage forks, R1 wiring, tests, WSL2 sync, one failed fit attempt, three applied shared-library diffs, four applied materials, the completed phase-4 fit and its R1 close, the M1 diagnostics persistence and its re-run, the mentor's closure gate) move no K. **Generation 1 for this bot ends here, closed by its own pre-registered kill criterion**, exactly as the Hypothesis (B1) said it would if no spec paid — R1 fired before phase 5 was even needed to say so.

### Phase-4 closure — mentor gate, 2026-09-10 (`ml4t-mentor` gate, recorded by `quant-orchestrator`, `reviews.jsonl` 2026-09-10T17:48:20Z, phase `"4 (gate: screen met, generation 1 closed)"`)

**Verdict: PHASE 4 MET AS A SCREEN** (`roadmap.md:146-147`: validation-split selection, trials in the registry, linear baseline present). R1 applied exactly as pre-declared: BH step-up over all 356 p-values (`12_model_analysis.py`, `ml4t.diagnostic` FDR at n=356), sign consistency = 4/4 folds in the series' own direction (`_model_reading.py::fold_ic_summary`), thresholds read from `setup.yaml::kill_criteria`, test-locked. Numbers confirmed byte-for-byte against `r1_early_close_verdict.json` and the registry (see the Decisions log row above for the re-confirmation after M1). Minimum BH-adjusted p-value across all 356: **0.994581**. Holdout guards fired correctly in all three notebooks (`06`:199-211, `07`:184-196, `12`:238-244): "latest validation end 2025-08-31 08:00 < holdout_start 2025-09-01: untouched".

**K reading: AGREED, K = 0 for the Deflated Sharpe Ratio.** No Sharpe was computed, nothing was selected by Sharpe. The fleet convention (`bots/exness_gold_sess/PHASE567_DECLARATION.md:24,29`) keeps TWO counters, and this bot now states them explicitly:

- **THREE-COUNTER RULE** (verbatim from the gate):
  - **FDR-n at the IC tier**: the size of the multiple-testing family a Benjamini-Hochberg correction is actually run over at the IC/screening level. **73** (phase 3, `05_evaluation`'s feature screen) then **356** (phase 4, `12_model_analysis`'s R1) — both **spent**, both real BH corrections over a real population, neither a DSR trial.
  - **DSR-K**: the count the Deflated Sharpe Ratio divides by, drawn only from a Sharpe-producing stage (`13_backtest` / `19_strategy_analysis`). **= 0** for this bot — no backtest has ever run.
  - **K5 = 2,136**: declared in advance (`setup.yaml::backtest.sweep`), **never drawn** — a generation that closes at phase 4 never reaches the stage that would draw it.
- **ACCUMULATION RULE** (verbatim from the gate): the 356 prediction sets already fit and scored join DSR-K the moment **(i)** any of them enters a Sharpe-producing stage (`K += 2,136`, the whole declared population, not a partial draw), **OR (ii)** any design element of a later generation is chosen because of a number read from this IC table (precedent: `PHASE567_DECLARATION.md:226`). `n_trials_effective_*`-style diagnostics never LOWER K once either trigger fires.

**Generation status: CLOSED by the pre-registered rule.** `kill_criteria.r1_early_close: true` fired exactly as approved. **B1 is NOT FALSIFIED** — its kill line is a phase-5 DSR statement at K5 plus a phase-6 breakeven statement, and NEITHER was drawn. The correct closure standard is the one `xau_fx_mt5`'s D1 line used ("INCONCLUSIVE by the pre-registered rule; line closed, no re-run"), which is WEAKER than `exness_fx_d1`'s leg (ii) closure (that one has a K-independent number backing it). This record MAY say: **mechanism (ii) of B1 (the conditioning hypothesis) measured roughly 4× short of the cost-paying IC before any backtest ran** (leading IC 0.0303 vs the 0.1144 threshold, 26.5 %). This record may **NOT** say "B1 falsified" or "no edge" — neither claim is what was measured. One descriptive note carried for the record, explicitly NOT evidence: 10 of the 28 `fwd_ret_24h` linear prediction sets have 4/4 negative folds, the same reversal sign phase 3 found in `slot_zscore_3` / `slot_ret_3`.

**Why opening phase 4 was right, restated**: R1 was approved as an early-close gate BEFORE the fit ran (approval predates the 2026-09-10T17:17 start), the fit cost 8 minutes 19 seconds and zero DSR trials, and phase 3 was a univariate feature screen while B1's mechanism (ii) is a multivariate claim only a fitted model population could test.

**GENERATION 2: NOT OPENED.** No re-run, no tuning — the following are explicitly PROHIBITED as a way to reopen this generation: more presets, more checkpoints, more horizons, a lower sign-consistency threshold, a different HAC/IC* choice, or re-reading the 356 already-scored rows for a different answer. A generation 2 is legitimate ONLY with:
1. **New user-approved hypothesis text** (a rewritten B1, explicitly approved, not a silent parameter change), stating a **pre-computed Minimum Detectable Effect (MDE) against IC\*** BEFORE any fit — what improvement over the measured 0.0303 would be worth spending a trial on;
2. **A new label or feature family.** The one candidate with a prior is the `on-chain and flow` family B1 itself names (`setup.yaml::features.families`) — **blocked by empty API keys in `.env`** (a data problem, not a design one; see Phase 0's own finding). No other declared family remains untried for this instrument;
3. **Or a new decision grid, as a separate case study** (a different cadence or a different instrument), not a parameter sweep inside this one.

Nothing in this bot's `setup.yaml`, training menus, or `kill_criteria` may be edited to chase a different R1 outcome on the SAME 356-row population. The M2 discrepancy (R1's code condition vs. the approved B2 text) must be reconciled — by the user amending B2's text, or by narrowing the code to match it exactly — before generation 2 opens, whatever generation 2 turns out to be.

**UPDATE 2026-09-11 (user decisions Q1/Q2/Q3, `reviews.jsonl` 2026-09-10T22:1xZ / UTC
2026-09-10T22:10Z):** M2's precondition above is MET — see §Kill criteria "M2 RESOLVED 2026-09-11"
above. Generation status changes from "generation 1 closed, generation 2 not planned" to
**CLOSED — generation 1 closed by R1; generation 2 CONDITIONAL** (Q2: not "retired"). See Phase
status row 4 for the full closure line and its two conditions. Q3: the user declined to source
on-chain data now, so condition (1) below stays open pending a future data decision; item 2 of
this list (a new user-approved B1 predicting `fwd_ret_8h` at the MDE read out in
`GEN2_DECISION_DRAFT_2026-09-11.md` §3) stays the other open condition. Item 3 (a new decision
grid / separate case study) remains an alternative path, not taken 2026-09-11.

## Open questions

1. **THE LEGACY V9 CONTINUUM BOT IS RUNNING RIGHT NOW, AND IT TRADES THIS BOT'S INSTRUMENT.** Measured 2026-09-08 07:13 local (UTC+7), which corrects the 2026-09-05 note in this file that said it was not running: PID **5960** running `python -m v9_continuum.main` plus a watchdog (PID 8416); `D:/05_Quant/v9 Continuum/logs/bot_status.json` reports `live_trading: true`, `dry_run: false`, `mt5_connected: true`, started `2026-09-07T22:13:39Z`, `magic_number: 202500`, `governor_status: OPERATIONAL`, one active cycle (NZDUSD), on the **same demo login 206539306** with balance 899.40 / equity 898.32 USD; `heartbeat.txt` was written seconds before the check. `D:/05_Quant/_archive/v9_continuum_original/config/symbols.py` carries `BTCUSD` in `SYMBOLS` (name `BTCUSDm`, category CRYPTO, line 99) **and** in `SUPER_ELITE_SYMBOLS` (line 109). Three consequences, none of them optional: (a) the holdout contamination flag in `setup.yaml::evaluation.legacy_v9_symbols` is not historical but **live and ongoing** — the holdout may CONFIRM, it may not SELECT, and no parameter of this bot may be chosen because it resembles a V9 rule; (b) any deployment of this bot shares equity, margin and a stop-out level with a bot that is trading now, which is what `bots/_shared/monitor/account_limits.yaml` exists for; (c) the magics differ (260904 vs 202500) so position queries will not cross, and that is asserted by `bots/tests/test_magic_numbers.py`. **For the user**: whether the legacy bot should keep running while this one is developed is a decision this file cannot make.
   **RESOLVED 2026-09-10** (user decision, recorded by `quant-orchestrator`): the legacy bot is **STOPPED**. Windows
   tasks `V9_Continuum_DailyPnL` and `V9_Continuum_EnsureBot` disabled, the running process tree (`start_bot.bat` →
   `scratch/watchdog.py` → `v9_continuum.main`, PID 5960 + watchdog PID 8416 as last measured 2026-09-08) terminated
   by the orchestrator, 0 open positions at the time of the stop. This closes the "is it running" question but does
   **not** retroactively clean the holdout: the legacy bot traded `BTCUSD` on this login for part of the declared
   holdout window (2025-09-01 → 2026-08-31) before being stopped today, so `setup.yaml::evaluation.legacy_v9_symbols`
   / `legacy_v9_contaminated_window` and consequence (a)-(c) above still stand unchanged — the holdout may CONFIRM,
   it may not SELECT. Demo equity read 891.15 USD (2026-09-10), down from 10,000 USD on 2026-09-05; user decided to
   reset the demo to 10,000 USD in the Exness Personal Area, **pending user action**.
2. **The minimum order is 88 % of the account — RESOLVED 2026-09-10, demo reset.** Originally: `volume_min` 0.01 lot × `contract_size` 1 = 0.01 BTC = **789 USD of notional** at the measured mid of 78,942, against **898 USD of equity** on the demo login — the smallest placeable order was ~88 % of the book, leaving volatility targeting, Kelly sizing and every risk overlay that scales a position exactly one step. **User re-funded the demo 2026-09-10** (the legacy V9 bot is stopped, see Decisions log 2026-09-10 "Legacy V9 Continuum ... STOPPED"): equity now **10,891.15 USD**. At that equity, 789 USD of notional is **≈ 7 % of the book**, not 88 % — sizing has roughly thirteen steps below full size rather than one. This CLOSES the capital-viability concern this open question tracked; `execution.share_type: fractional` still stands (the engine-rounding question is separate and unaffected by the equity level). Phase 6 may proceed to declare `capital.min_viable_allocated` against this reading rather than treating the instrument as unsizable. Compare `exness_fx_d1`'s own version of this (BOT.md open question 26: 1,160 USD of EURUSD notional per 0.01 lot) — that bot's equity was not re-funded in this update and its ratio is unchanged.
3. **Every cost number is from the DEMO login.** The spread, the swap and the contract terms were all read on `206539306`. The demo's `swap_short = 0.0` against `swap_long = -1638.6` is unusual enough to treat as unverified — a real Pro account charging both sides would change the sign of every long-short comparison in phase 5. `16_costs` must not run before the real account is read. (Same open item as `exness_fx_d1` open question 1 and `exness_gold_sess`.)
4. **The per-bar `spread` field's semantics are not documented.** It is the broker's own record for a bar and it is point-in-time, which is what the cost design needs; whether MT5 stamps the bar's opening, minimum or typical spread is not stated anywhere this repository can check. The 8-hour fold takes the **first** H4 bar's spread (`resample_4h_to_8h`, MT5's own convention), and the census over all 18,756 raw H4 bars agrees with the decision-grid census to within 0.2 bp, which is what says the fold is not selecting a cheap half of each slot. If the field turns out to be the bar's *minimum*, every in-sample cost here is a floor.
5. **The price loader for phase 5 — REGISTERED AND WIRED 2026-09-10 (fleet mentor gate #5).** `case_studies/utils/backtest_loaders.py::_PRICE_CONFIG` now carries an `"exness_btc_8h"` entry in the REAL schema (`entity_col`/`time_col`/`close_col`/`ohlcv`/`loader`/`drop_cols` — the mentor's correction to a first, wrong proposal that used `symbols`/`timeframe`/`close_keyed`, a schema that does not exist anywhere else in `_PRICE_CONFIG`), `loader: "exness_btc_8h_8h"`, dispatched by a new branch in `_load_via_canonical` (`backtest_loaders.py`, inserted after the `nasdaq100_bars` branch, before the `Unknown loader` fallback) that reuses `case_studies.exness_btc_8h._features.decision_grid` on `load_mt5_bars("8h", ...)` rather than reimplementing the +480-minute close shift inline. **A real PIT leak was found and fixed while writing the diff, not by inspection**: `load_mt5_bars(end_date=...)` bounds the raw, OPEN-keyed bars inclusively through the whole `end_date` day, so the LAST folded 8h bar of a window ending `2025-08-31` opens at 16:00 that day and, once `decision_grid` shifts it to its CLOSE, lands at `2025-09-01T00:00` — one decision instant INTO the holdout, `>= holdout_start`. The branch now re-windows explicitly on the CLOSE side after the shift (`panel.filter(timestamp < end_date + 1 day)`), the exact pattern `exness_usidx_sess_h1`'s own branch already uses for the same reason. Pinned by `bots/exness_btc_8h/tests/test_price_config_diff.py::TestCloseKeyedGridItem3::test_naive_end_date_bound_leaks_one_close_keyed_row_past_holdout_start`, which reproduces the un-clipped leak and proves the clip removes exactly the one leaked row.

   **Combined re-gate, 2026-09-10, materials #1 and #3, APPLIED.** #4 and #5 were MET on the mentor's re-gate; two materials followed. **Material #3** (`backtest_loaders.py` PIT clip): the clip coerced `end_date` for the RE-WINDOW only, but passed the RAW `end_date` straight into `load_mt5_bars(...)` first — a real bug this material's own required test caught: `load_mt5_bars` expects a bare string and raised `SchemaError` on a `date`/`datetime` object. Fixed with `_naive_end_date_string(value)` (module-level helper, `backtest_loaders.py`, before `_load_via_canonical`) — coerces `date` / naive `datetime` / TZ-aware `datetime` (UTC-converted first) / string to a bare `"YYYY-MM-DD"`, computed ONCE and reused for both the `load_mt5_bars` call and the re-window clip; a comment states the clip only fires when `end_date` is given. **Material #1** (`backtest_runner.py`, hard-coded `case_study == "exness_btc_8h" and book == "all"`): made declarative — `setup.yaml::decision.session_filter_all_value: all` now names which declared book is the whole-grid no-op, and `apply_session_filter` reads it instead of the case-study literal; a case study with no such key (`exness_gold_sess`) never takes the no-op branch. Proven behaviourally: `TestAllValueIsDeclaredNotHardCodedMaterial1` monkeypatches the declaration away and shows `"all"` STOPS being a no-op (raises the empty-frame guard), then restores it and shows the no-op returns.

   **Verified after both fixes** (native run, repo copy, no `ML4T_OUTPUT_DIR`, no sync): `test_session_filter_diff.py` **20 passed, 0 skipped**; `test_price_config_diff.py` **18 passed, 1 skipped** (skip is labels not built, unrelated to either fix).

   **Verified** (`case_studies/utils/backtest_loaders.py:794-808` the `_PRICE_CONFIG` entry, `:1092-1131` the dispatch branch): `bots/exness_btc_8h/tests/test_price_config_diff.py` **10 passed, 0 skipped** (was 6 passed / 4 skipped before the diff — the registration-gated items now run for real: key-set equality against the real schema, loader resolves in dispatch, grid identity against `02_labels`' own grid, close-keying, the PIT clip, legacy `fx_pairs`/`exness_gold_sess` entries byte-identical). The 24-hour label's Ch19 granularity note stands unchanged: the registered grid is the 8-hour decision grid, still three rows for a `fwd_ret_24h` hold.

   **The session-filter half — ALSO REGISTERED AND WIRED 2026-09-10 (fleet mentor gate #4).** `case_studies/utils/backtest_runner.py::apply_session_filter` (`:785-885`) now accepts `case_study in ("exness_gold_sess", "exness_btc_8h")` and dispatches to `case_studies.exness_btc_8h._features.session_book_of` for `no_swap_night` / `us_hours`. **The mentor's finding was a real bug in the first proposal**: `session_book_of` is a two-way partition and never returns `"all"`, so a resolver that matched `book == "all"` against it would filter every row out and raise the empty-frame guard. The applied diff treats `book == "all"` (on `exness_btc_8h` only) as an EXPLICIT no-op, validated against `decision.session_filter_values` first (so a typo still raises) and then skipping resolution entirely rather than being matched against the resolver. `case_studies/exness_btc_8h/13_backtest.py` was fixed the same day to pass `session_filter: "all"` explicitly for every book (no longer omitting the key and relying on the generic empty-filter short-circuit), so the registered spec says what it means.

   **CLOSED-BY-MATERIALS 2026-09-10**: materials (1) and (3) (the combined re-gate's non-blocking follow-ups on top of this item's already-MET diffs #4/#5) are applied and re-verified by both a WSL2/`ML4T_OUTPUT_DIR` run and a native run (see Decisions log 2026-09-10); the only open residual is environmental (WSL2 experiment/mirror config not yet synced with material (1), blocked by the concurrent `exness_fx_d1` sweep), not a defect in this item.

   **Verified**: `bots/exness_btc_8h/tests/test_session_filter_diff.py` **18 passed, 0 skipped** (was 15 passed / 3 skipped before the diff): gold's own dispatch is frame-identical to a direct `session_of` filter (london, ny, drop_friday — real regression, unchanged by the diff), every other registered case study still raises the original message, `"all"` is a true no-op (`rows(all) == rows(no_swap_night) + rows(us_hours)`, union equals the unfiltered frame), an undeclared book still raises via the generic `declared` check, and the three books are pairwise different. Both diffs are ADDITIVE: no existing case study's registered spec, hash or result moved (proven by the "every other case study still raises" and "existing `_PRICE_CONFIG` entries unchanged" tests, both of which ran unmodified before and after).
6. **`rebalance_step` is right on the pooled book and wrong on the filtered ones.** `fwd_ret_24h: 3` is exactly one decision a day on the unfiltered grid. On `us_hours` the resolved schedule is 1 slot a day, so a step of 3 walks three days; on `no_swap_night` it is 2 a day, so a step of 3 walks a day and a half. `13_backtest` must print `resolve_rebalance_timestamps` on the FILTERED grid before any sweep — the `nasdaq100_microstructure` failure, "the values did not change; what they count did". Same for `signal_specs[1].bars_per_day`, which is declared `null` in `setup.yaml` precisely so it cannot be inherited: it is 3, 2 and 1 on the three books.
7. **`pays_swap_night` and `slot_of_day` are the same column** on an unbroken grid, and `05_evaluation`'s redundancy tree confirmed it (both in cluster 25 with `is_us_hours`). Both are kept because they carry different meanings to a reader and the tree, not the author, is what decides. They would stop being the same only if the broker moved its rollover or the grid acquired a hole; the second is asserted by the data-quality test and the first is not.
8. **Three columns are constants inside a filtered book.** `slot_of_day`, `is_us_hours` and `pays_swap_night` take one value inside `us_hours`; the last two also inside `no_swap_night`. They are informative on the pooled book and are not dropped, but a phase-5 spec that filters to one book and keeps them is fitting on a constant. The ledger carries `constant_in_a_book` for exactly this.
9. **The `usidx` family is null on the first nine months of the development window.** USTEC daily bars start 2019-07-16 on this account, so `usidx_ret_63d` and `usidx_beta_63d` are null until 2019-09-18 while the matrix opens 2018-12-22 — 810 of 7,335 development rows, 11.0 %, all before the oldest training window's second week. Declared in `features.null_policy.may_be_null` with its cap before any IC was read. The linear menu's declared answer is median imputation inside the fold pipeline; LightGBM reads a null as a branch.
10. **The `.ipynb` files carry no outputs.** They were generated from the `.py` sources with `jupytext --to notebook` and `scripts/sync_notebooks.py --check` reports 0 drift on all five, but the stages were run **headless from the `.py`**, so the notebooks have never been executed. A committed `.ipynb` in this repository must be re-executed and provenance-stamped; nothing here has been committed, and this must be done before anything is.
11. **`pytest bots` still fails to collect `exness_usidx_sess`.** `ModuleNotFoundError: No module named 'tests.test_data_quality'` — that bot has no `bots/exness_usidx_sess/__init__.py`, so its test modules resolve under the top-level package name `tests`. Reproduced with this bot excluded, so it is not caused by it; `exness_gold_sess` had the same fault and was fixed as a side effect of adding this bot's marker. With `--ignore=bots/exness_usidx_sess` the whole tree is **319 passed, 72 skipped** (2026-09-08). For whoever owns that bot.
12. **`evaluation.periods_per_year: 365` has not been exercised.** It annualises the engine's daily return series and `reconcile_periods_per_year` will compare it against the observed grid the first time a backtest runs. On a 24/7 daily grid the observation should be ~365 and the declared value should stand; on this instrument the reconciler's 0.5×–2× band would also have accepted 1,095, which is why `decision.slots_per_year` is a separate key and why this is worth checking rather than assuming when phase 5 opens.
13. **The strongest single relationship found is a one-day reversal, and it does not pay.** `slot_zscore_3` and `slot_ret_3` both carry a time-series IC of about **−0.041** with the same sign in all four validation folds, HAC t ≈ −2.5 to −2.6 and a block-bootstrap interval excluding zero. **0 of 73 columns survive Benjamini-Hochberg** over the declared set. Against IC\*, the correlation a signal needs before it pays the in-sample spread plus one night of financing, the best column is **0.36×** what it would have to be. Two IC\* figures are reported and neither is wrong: **0.0954** in `01_feasibility_analysis` and `02_labels`, on the whole development window (label σ = 190 bps), and **0.1144** in `05_evaluation`, on the validation slots it actually scored (σ smaller, so the bar is higher). Both are DERIVED from `setup.yaml` in the notebook rather than typed, after a literal 0.0965 went stale the first time the declared spread moved. That is not a reason to stop (an IC on one column is not the IC of a model over 68, and a backtest charges costs on trades rather than on every slot) but it is the number every phase-4 and phase-5 result has to be read against, and it belongs in the Hypothesis the mentor is drafting.
14. **`utils/modeling.py::load_modeling_dataset` entity-column fix — APPLIED 2026-09-10 (fleet mentor gate #6), DECLARATION-driven, not emptiness-driven.** First measured 2026-09-10 (first fit attempt, see Decisions log "06_linear fit attempt, blocked"): the shared function (top-level `utils/`, "the canonical data loading function for ALL Ch11+ modeling notebooks") filtered `entity_cols` to those with `features[c].n_unique() > 1` (`utils/modeling.py:924`), to avoid a genuinely constant column (e.g. a straddle instrument id) polluting a cross-sectional IC. On a ONE-instrument panel `symbol` is constant EVERYWHERE by design, not by data loss, so the filter emptied `entity_cols` and `case_studies/utils/linear.py:544` (`_load_batch_base`) raised `ValueError: linear runner requires timestamp and an entity key` before any fold could fit. No prior Exness-fleet bot reached this code path (`exness_gold_sess` 2 instruments, `exness_fx_d1` 5, `exness_usidx_sess` 2); `exness_btc_8h` is the first ONE-instrument case study in the fleet.

    **The mentor rejected the first proposal** (fall back to the unfiltered list whenever the filtered one is empty) as NOT_EVIDENCE_YET: emptiness alone cannot distinguish a genuinely one-instrument universe from a `max_symbols=1` PREVIEW reduction on a multi-asset case study, and `etfs`/`fx_pairs` under such a reduction correctly raise today (`linear.py:543-544`) — the naive fix would have silently made that raise disappear too. **The applied fix reads the case study's OWN declaration** (`setup.yaml::universe.n_assets`, the one key every registered case study declares — measured 2026-09-10: `etfs` 100, `fx_pairs` 20, `crypto_perps_funding` 19, `exness_gold_sess` 2, `exness_fx_d1` 5, `exness_btc_8h` 1) through `utils.artifact_specs.load_setup_config(case_study_id)`, already in scope inside the function, and keeps the constant entity column only when `n_assets == 1`:
    ```python
    # utils/modeling.py, inside load_modeling_dataset, applied 2026-09-10:
    _non_constant_entity_cols = [c for c in entity_cols if features[c].n_unique() > 1]
    if not _non_constant_entity_cols:
        from utils.artifact_specs import load_setup_config
        _declared_universe = load_setup_config(case_study_id).get("universe") or {}
        _declared_one_symbol_universe = int(_declared_universe.get("n_assets", 0) or 0) == 1
        if _declared_one_symbol_universe and entity_cols:
            _non_constant_entity_cols = entity_cols
    entity_cols = _non_constant_entity_cols
    ```
    A `max_symbols=1` preview reduction on `etfs`/`fx_pairs` still raises, unchanged: their OWN `universe.n_assets` is 100/20, so `_declared_one_symbol_universe` is `False` regardless of what one query happened to reduce to. `exness_gold_sess`'s uncommitted `classification_eval_label` diff (a concurrent, unrelated edit by that bot's builder, in force while this diff was applied) was left untouched — this fix touches only the entity-column block.

    **Combined re-gate, 2026-09-10, material #2, APPLIED.** #6's blast radius was confirmed zero by static proof (only `exness_btc_8h` declares `universe.n_assets: 1`), conditional on the positive test passing in WSL2 once `exness_fx_d1`'s builder syncs and runs it (not done by this bot's writer — no sync performed here). **Material #2**: when the single-asset branch fires, assert `int(n_assets) == len(universe.symbols)` and raise on mismatch — TWO independently-typed declarations in `setup.yaml::universe` (`n_assets` and `symbols`) with nothing upstream enforcing they agree; trusting `n_assets` alone would silently keep a constant entity column even if `symbols` had drifted to two entries without the count being updated. Applied directly below the `_declared_one_symbol_universe` check (`utils/modeling.py`): raises `ValueError` naming both declared values before the fallback ever fires. Tested: `bots/exness_btc_8h/tests/test_entity_cols_diff.py::TestTwoSourcesOfTruthMaterial2` — this bot's real declaration is self-consistent (`n_assets == len(symbols) == 1`); a monkeypatched synthetic mismatch (`n_assets: 1`, `symbols: [BTCUSD, ETHUSD]`) raises with the expected message.

    **Combined re-gate, material #4** (`bots/exness_btc_8h/tests/test_entity_cols_diff.py`): the skip-detection helper caught ANY exception from `load_modeling_dataset` and reported one reason for two different facts - "this bot's data is not built here" (an environment fact, correctly a skip) and "the diff is missing or reverted while data IS present" (a real regression, must FAIL). Rewritten as a tri-state probe (`_entity_cols_state_for_btc`, `state ∈ {"fixed", "missing_data", "not_fixed"}`): only `FileNotFoundError` yields `"missing_data"` (skipped); an empty `entity_cols` with no exception yields `"not_fixed"` and the gated tests now RUN UNSKIPPED, so they fail for real if the diff is ever reverted. **Verified** (native run, repo copy, no `ML4T_OUTPUT_DIR`): `bots/exness_btc_8h/tests/test_entity_cols_diff.py` **4 passed, 11 skipped**, every skip reading `"exness_btc_8h data not built in this environment: ... FileNotFoundError(...)"` — the correct, honest reason in an environment that genuinely has no built labels/features and was never synced.

    **Verified**: `bots/exness_btc_8h/tests/test_entity_cols_diff.py` **9 passed, 4 skipped** (skips are `fx_pairs`/`etfs` data not being built in this environment, not the fix — the fix's own gate, probed live against `exness_btc_8h`, flipped from "still empty" to `entity_cols=['symbol']` the moment the diff landed). `exness_btc_8h` now yields `entity_cols == ["symbol"]` and clears the linear runner's own gate (`case_studies/utils/linear.py:543-544`) that used to raise. This was a HARDER blocker than the session-filter / price-config pair above: those block `13_backtest` only; this one blocked `06_linear` and therefore `07_gbm` and `12_model_analysis` too — the entire phase-4 fit. Applied on an empty `case_studies/*/run_log/` for every case study (true before and after), so it enters no existing hash and changes no training identity — confirmed by `TestEntityColsNotInTrainingIdentityItem6`, 2/2 passed.

    **CLOSED-BY-MATERIALS 2026-09-10**: materials (2) and (4) (the combined re-gate's non-blocking follow-ups on top of this item's already-APPLIED diff #6) are applied and re-verified by both a WSL2/`ML4T_OUTPUT_DIR` run and a native run (see Decisions log 2026-09-10); no residual gap in this item.
15. **Generation 2 conditions.** Full package: `bots/exness_btc_8h/GEN2_DECISION_DRAFT_2026-09-11.md` (mentor draft the user decided on 2026-09-11, §§1-6). Generation 2 is CONDITIONAL (Phase status row 4; Decisions log 2026-09-11) on BOTH:
    1. **A user data decision for the `on-chain and flow` family** — vendor (e.g. Glassnode / CryptoQuant / Coin Metrics for exchange netflow and miner reserves; ETF flow exists only from 2024-01-11), a new key in `.env`, a backfill covering 2018-02-16 → 2025-08-31 (the development window) and a vintage record (Ch02 `14_point_in_time_validation`). **User declined 2026-09-11 (Q3: not now)** — this condition stays open, no vendor chosen.
    2. **A new user-approved B1** predicting `fwd_ret_8h`, stating a pre-computed Minimum Detectable Effect BEFORE any fit: pooled IC ≥ **0.062** at m = 36 (a-priori menu, e.g. "ols + two default GBM presets × 6 checkpoints") or ≥ **0.071** at m = 356 (the full generation-1 population size), at HAC bandwidth 9, against the measured `hac_se` (median **0.01937** pooled, **0.01535** on `fwd_ret_8h` alone) — draft §3's MDE table, Ch07 `06_ic_inference`/`07_multiple_testing` method. No tuning of preset, checkpoint, horizon, HAC or IC\* to chase a different reading of the SAME 356 rows; `dollar` family is "scored AND spent", not available as a new family.
    Neither condition is met as of 2026-09-11. The draft's path (ii) (a new decision grid / separate case study, e.g. `exness_btc_d1`) remains a documented alternative, not chosen.
